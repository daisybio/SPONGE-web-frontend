openapi: 3.0.0
info:
  description: This is the swagger file that goes with our server code for the sponge REST-API
  version: 2.0.0
  title: SPONGE REST-API documentation
servers:
  - url: /sponge-api
    description: Main (production) server
  - url: /sponge-api-dev
    description: Internal staging server for testing
components:
  parameters:
    VersionParam:
      name: sponge_db_version
      in: query
      description: The version of the API. The default value is specified in the python functions!
      required: false
      schema:
        type: string
        nullable: true
  schemas:
    TranscriptSchema:
      type: object
      properties:
        enst_number:
          type: string
          description: Enst number of the transcript.
        gene:
          type: object
          description: Gene the transcript belongs to.
          properties:
            ensg_number:
              type: string
              description: Ensg number of the gene.
            gene_symbol:
              type: string
              description: From official gene nomenclature committees such as HGNC (for human). Source - Ensembl - only provided if available.
        canonical_transcript: 
          type: integer
          description: Is a canonical transcript
        transcript_type:
          type: string
          description: The transcript type includes both status (e.g. known) and biotype (e.g. protein coding).
        start_pos:
          type: integer
          description: Starting position of the gene.
        end_pos:
          type: integer
          description: Endposition of the gene.
    GeneInteractionDatasetLongSchema:
      type: object
      properties:
        correlation:
          type: number
          description: Correlation of the corresponding ceRNA interaction.
        mscor:
          type: number
          description: Multiple sensitivity correlation (mscor) of the corresponding ceRNA interaction.
        p_value:
          type: number
          description: FDR adjusted p-value of the corresponding ceRNA interaction.
        sponge_run:
          type: object
          description: Information about specific run and corresponding cancer type/dataset.
          properties:
            dataset:
              type: object
              properties:
                data_origin:
                  type: string
                  description: Origin of the dataset.
                dataset_ID:
                  type: integer
                  description: Internal database ID of the cancer type/dataset.
                disease_name:
                  type: string
                  description: Name of the cancer type/dataset.
            sponge_run_ID:
              type: integer
              description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
        gene1:
          type: object
          description: One of the two interacting ceRNAs.
          properties:
            ensg_number:
              type: string
              description: Ensg number of the gene.
            gene_symbol:
              type: string
              description: From official gene nomenclature committees such as HGNC (for human). Source - Ensembl - only provided if available.
        gene2:
          type: object
          description: The other part of the ceRNA interaction.
          properties:
            ensg_number:
              type: string
              description: Ensg number of the gene.
            gene_symbol:
              type: string
              description: From official gene nomenclature committees such as HGNC (for human). Source - Ensembl - only provided if available.
    TranscriptInteractionDatasetLongSchema:
      type: object
      properties:
        correlation:
          type: number
          description: Correlation of the corresponding ceRNA interaction.
        mscor:
          type: number
          description: Multiple sensitivity correlation (mscor) of the corresponding ceRNA interaction.
        p_value:
          type: number
          description: FDR adjusted p-value of the corresponding ceRNA interaction.
        sponge_run:
          type: object
          description: Information about specific run and corresponding cancer type/dataset.
          properties:
            dataset:
              type: object
              properties:
                data_origin:
                  type: string
                  description: Origin of the dataset.
                dataset_ID:
                  type: integer
                  description: Internal database ID of the cancer type/dataset.
                disease_name:
                  type: string
                  description: Name of the cancer type/dataset.
            sponge_run_ID:
              type: integer
              description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
        transcript1: 
          $ref: '#/components/schemas/TranscriptSchema'
        transcript2: 
          $ref: '#/components/schemas/TranscriptSchema'
    NetworkAnalysisSchema:
      type: object
      properties:
        betweenness:
          type: number
          description: Betweenness of the corresponding gene (considered as node) in the specific network.
        eigenvector:
          type: number
          description: Eigenvector of the corresponding gene (considered as node) in the specific network.
        node_degree:
          type: number
          description: Degree of the corresponding gene (considered as node) in the specific network.
        gene:
          type: object
          description: Information about the gene that satisfies all filters.
          properties:
            ensg_number:
              type: string
              description: Ensg number of the gene.
            gene_symbol:
              type: string
              description: From official gene nomenclature committees such as HGNC (for human). Source - Ensembl - only provided if available.
        sponge_run:
          type: object
          description: Information about specific run and corresponding cancer type/dataset.
          properties:
            dataset:
              type: object
              properties:
                data_origin:
                  type: string
                  description: Origin of the dataset.
                dataset_ID:
                  type: integer
                  description: Internal database ID of the cancer type/dataset.
                disease_name:
                  type: string
                  description: Name of the cancer type/dataset.
            sponge_run_ID:
              type: integer
              description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
    NetworkAnalysisSchemaTranscript:
      type: object
      properties:
        betweenness:
          type: number
          description: Betweenness of the corresponding gene (considered as node) in the specific network.
        eigenvector:
          type: number
          description: Eigenvector of the corresponding gene (considered as node) in the specific network.
        node_degree:
          type: number
          description: Degree of the corresponding gene (considered as node) in the specific network.
        transcript:
          type: object
          description: Information about the transcript that satisfies all filters.
          properties:
            enst_number:
              type: string
              description: Enst number of the transcript.
            gene:
              type: object
              description: Gene the transcript belongs to.
              properties:
                ensg_number:
                  type: string
                  description: Ensg number of the gene.
                gene_symbol:
                  type: string
                  description: From official gene nomenclature committees such as HGNC (for human). Source - Ensembl - only provided if available.
        sponge_run:
          type: object
          description: Information about specific run and corresponding cancer type/dataset.
          properties:
            dataset:
              type: object
              properties:
                data_origin:
                  type: string
                  description: Origin of the dataset.
                dataset_ID:
                  type: integer
                  description: Internal database ID of the cancer type/dataset.
                disease_name:
                  type: string
                  description: Name of the cancer type/dataset.
            sponge_run_ID:
              type: integer
              description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
paths:
  /diseases: 
    get: 
      operationId: dataset.get_diseases
      tags:
        - Dataset
      summary: Get information on all diseases
      description: Get information on all diseases
      parameters:
        - name: disease_ID
          in: query
          description: Internal database ID of the disease.
          required: false
          schema:
            type: integer
        - name: disease_name
          in: query
          description: Name of the disease.
          required: false
          schema:
            type: string
        - name: disease_subtype
          in: query
          description: Subtype of the disease.
          required: false
          schema:
            type: string
        - name: versions
          in: query
          description: List of sponge_db_versions in which the disease need to be present.
          required: false
          schema:
            type: array
            items: 
              type: string
      responses:
        "200":
          description: Successfully read information about all diseases.
          content:
            application/json:
              schema:
                type: object
                properties:
                  disease_name:
                    type: string
                    description: Name of the disease
                  disease_subtype:
                    type: string
                    description: Subtype of the disease
                  data_origin:
                    type: string
                    description: Name of the data origin (e.g. TCGA)
  /datasets:
    get:
      operationId: dataset.get_datasets
      tags:
        - Dataset
      summary: Get information on one or more datasets
      description: Get information on one or more datasets
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: dataset_ID
          in: query
          description: Internal database ID of the cancer type/dataset.
          required: false
          schema:
            type: integer
        - name: disease_name
          in: query
          description: Name of the disease
          required: false
          schema:
            type: string
        - name: disease_subtype
          in: query
          description: Subtype of the disease. Search for "unspecific" to get all datasets without subtype.
          required: false
          schema:
            type: string
        - name: data_origin
          in: query
          description: Name of the data origin (e.g. TCGA)
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successfully read information about datasets of given source.
          content:
            application/json:
              schema:
                type: object
                properties:
                  dataset_ID:
                    type: integer
                    description: Internal database ID of the cancer type/dataset.
                  disease_name:
                    type: string
                    description: Name of the cancer type/dataset.
                  data_origin:
                    type: string
                    description: Cancer type/dataset origin (database name, etc.).
                  disease_type:
                    type: string
                    description: Type of the disease (e.g. cancer).
                  download_url:
                    type: string
                    description: URL to cancer type/dataset annotation.
                  disease_subtype:
                    type: string
                    description: Subtype of specific cancer type ('null' if no relevant subtype is present)
                  sponge_db_version:
                    type: integer
                    description: SPONGEdb version used
                  sample_count: 
                    type: integer
                    description: Number of samples in the dataset.
  /get_disease_from_sample:
    get:
      operationId: dataset.get_disease_from_sample
      tags:
        - Dataset
      summary: Get the disease name from a sample ID
      description: Get the disease name sample ID. The Tissue Source Site Code of the TCGA sample ID is used to identify the disease of the specific sample. 
      parameters:
        - name: sample_ID
          in: query
          description: Sample ID of the cancer type/dataset. If not specified, all diseases will be returned.
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successfully read information about the disease from a sample ID.
          content:
            application/json:
              schema:
                type: object
                description: Mapping of sample ID to disease name. If no sample ID is provided, mapping of TSS code to disease name is returned.
  /dataset/spongeRunInformation:
    get:
      operationId: dataset.read_spongeRunInformation
      tags:
      - Dataset
      summary: Get all run information about the cancer type/dataset of interest.
      description: Retrieve all used parameters of the SPONGE method to create published results for the cancer type/dataset of interest.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      responses:
        "200":
          description: Successfully read all used parameters for the dataset of interest.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    coefficient_direction:
                        type: string
                        description: If "<", coefficient has to be lower than coefficient.threshold, if ">", coefficient has to be larger than threshold. If NULL, the absolute value of the coefficient has to be larger than the threshold.
                    coefficient_threshold:
                        type: string
                        description: Threshold to cross for a regression coefficient to be called significant. Depends on the parameter coefficient.direction.
                    dataset:
                        type: object
                        description: Information about the cancer type/dataset.
                        properties:
                          dataset_ID:
                            type: integer
                            description: Internal database ID of the cancer type/dataset.
                          disease_name:
                            type: string
                            description: Name of the cancer type/dataset.
                        required:
                          - dataset-ID
                          - disease_name
                    f_test:
                        type: boolean
                        description: If true (1), an F-test is performed on each model parameter to assess its importance for the model based on the RSS of the full model vs the RSS of the nested model without the miRNA in question.
                    f_test_p_adj_threshold:
                        type: number
                        description: If F.test is TRUE (1), threshold to use for miRNAs to be included.
                    ks:
                        type: string
                        description: A sequence of gene-gene correlation values for which null models are computed.
                    log_level:
                        type: string
                        description: The log level, can be one of "info", "debug", "error".
                    m_max:
                        type: integer
                        description: null models are build for each elt in ks for 1 to m_max miRNAs
                    min_corr:
                        type: number
                        description: Consider only gene pairs with a minimum correlation specified here.
                    number_of_datasets:
                        type: integer
                        description: The number of mscor coefficients to be sampled from each covariance matrix.
                    number_of_samples:
                        type: integer
                        description: The number of samples available in the expression data.
                    sponge_run_ID:
                        type: integer
                        description: Internal database ID of the specific parameter setting (=run).
                    variance_cutoff:
                        type: string
                        description: Only consider genes and miRNA with variance > var.threshold. If this parameter is NULL no variance filtering is performed (Parameter name in SPONGE method is var.threshold)
  /networkResults:
    get:
      operationId: network_results.get_network_results
      tags:
      - network_results
      summary: Get information about all cancer types/datasets or specific one.
      description: Get information about all available datasets to start browsing or search for a specific cancer type/dataset.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type.
        required: false
        schema:
          type: string
      - name: level
        in: query
        description: gene or transcript
        required: false
        schema:
          type: string
      responses:
        "200":
          description: Successfully read network result information about all cancer types and subtypes if avaliable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: object
                    properties:
                      scores:
                        type: object
                        properties:
                          labels:
                            type: array
                            description: list of cancer type names
                          values:
                            type: object
                            description: matrix of hubness scores
                      euclidean_distances:
                        type: object
                        properties:
                          x:
                            type: array
                            description: list of x coordinates
                          y:
                            type: array
                            description: list of y coordinates
                          labels:
                            type: array
                            description: list of cancer type names
                  subtype:
                    type: object
                    properties:
                      scores:
                        type: object
                        properties:
                          labels:
                            type: array
                            description: list of cancer type names
                          values:
                            type: object
                            description: matrix of hubness scores
                      euclidean_distances:
                        type: object
                        properties:
                          x:
                            type: array
                            description: list of x coordinates
                          y:
                            type: array
                            description: list of y coordinates
                          labels:
                            type: array
                            description: list of cancer type names
  /ceRNAInteraction/checkGeneInteraction:
    get:
      operationId: geneInteraction.testGeneInteraction
      tags:
        - ceRNANetwork
      summary: Tests for specific gene  if has at least one interaction in each cancer type/dataset.
      description: Tests for specific gene (identified by ensg number or gene symbol) if has at least one interaction in each cancer type/dataset.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: ensg_number
        in: query
        description: Ensg number of the gene of interest (e.g. ENSG00000259090).
        required: false
        schema:
          type: string
      - name: gene_symbol
        in: query
        description: Symbol of the gene of interest (e.g. SEPT7P1).
        required: false
        schema:
          type: string
      responses:
        "200":
          description: Tests for specific gene (identified by ensg number or gene symbol) if has at least one interaction in each cancer type/dataset.
          content:
            application/json:
              schema:
                type: object
                items:
                  properties:
                    data_origin:
                      type: string
                      description: Origin of the dataset.
                    disease_name:
                      type: string
                      description: Name of the cancer type/dataset.
                    include:
                      type: integer
                      description: Result of the test. 0 means the gene is not included in the corresponding ceRNA interaction network. 1 mean the gene is a part of the ceRNA network.
                    sponge_run_ID:
                      type: integer
                      description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
  /ceRNAInteraction/checkTranscriptInteraction:
    get:
      operationId: transcriptInteraction.test_transcript_interaction
      tags:
        - ceRNANetwork
      summary: Tests for specific transcript  if has at least one interaction in each cancer type/dataset.
      description: Tests for specific transcript (identified by ensg number or gene symbol) if has at least one interaction in each cancer type/dataset.
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: dataset_ID
          in: query
          description: Internal database ID of the cancer type/dataset.
          required: false
          schema:
            type: integer
        - name: enst_number
          in: query
          description: Enst number of the transcript of interest.
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Tests for specific gene (identified by ensg number or gene symbol) if has at least one interaction in each cancer type/dataset.
          content:
            application/json:
              schema:
                type: object
                items:
                  properties:
                    data_origin:
                      type: string
                      description: Origin of the dataset.
                    disease_name:
                      type: string
                      description: Name of the cancer type/dataset.
                    include:
                      type: integer
                      description: Result of the test. 0 means the gene is not included in the corresponding ceRNA interaction network. 1 mean the gene is a part of the ceRNA network.
                    sponge_run_ID:
                      type: integer
                      description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
  /ceRNAInteraction/findAll:
    get:
      operationId: geneInteraction.read_all_genes
      tags:
      - ceRNANetwork
      summary: Get all ceRNA interactions the gene(s) of interest is/are involed in.
      description: Get all ceRNA interactions by given identifications (ensg_number, gene_symbol or gene_type), specific cancer type/dataset or different filter possibilities according different statistical values (e.g. FDR adjusted p-value).
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg number(s) of the gene(s) of interest (e.g. ENSG00000259090, ENSG00000217289).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene(s) of interest (e.g. SEPT7P1,TIGAR).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_type
        in: query
        description: Defines the type of gene of interest (only one possibility choosable).
        required: false
        schema:
          type: string
          enum:
          - 3prime_overlapping_ncRNA
          - antisense
          - antisense_RNA
          - bidirectional_promoter_lncRNA
          - IG_C_gene
          - IG_C_pseudogene
          - IG_V_gene
          - IG_V_pseudogene
          - lincRNA
          - macro_lncRNA
          - miRNA
          - misc_RNA
          - Mt_rRNA
          - polymorphic_pseudogene
          - processed_pseudogene
          - processed_transcript
          - protein_coding
          - pseudogene
          - ribozyme
          - rRNA
          - rRNA_pseudogene
          - scaRNA
          - scRNA
          - sense_intronic
          - sense_overlapping
          - snoRNA
          - snRNA
          - TEC
          - TR_C_gene
          - TR_V_gene
          - TR_V_pseudogene
          - transcribed_processed_pseudogene
          - transcribed_unitary_pseudogene
          - transcribed_unprocessed_pseudogene
          - translated_processed_pseudogene
          - unitary_pseudogene
          - unprocessed_pseudogene
          - vaultRNA
      - name: pValue
        in: query
        required: false
        description: Threshold of the FDR adjusted p-value. Default is 0.05.
        schema:
          type: number
          default: 0.05
      - name: pValueDirection
        in: query
        required: false
        description: Direction of the FDR adjusted p-value threshold (<=, >=).
        schema:
          type: string
          enum:
          - <
          - ">"
          default: <
      - name: mscor
        in: query
        required: false
        description: Threshold of the 'multiple sensitivity correlation' (mscor).
        schema:
          type: number
      - name: mscorDirection
        in: query
        required: false
        description: Direction of the mscor threshold (<=, >=).
        schema:
          type: string
          enum:
          - <
          - ">"
          default: <
      - name: correlation
        in: query
        required: false
        description: Threshold of the correlation.
        schema:
          type: number
      - name: correlationDirection
        in: query
        required: false
        description: Direction of the correlation threshold (<=, >=).
        schema:
          type: string
          enum:
          - <
          - ">"
          default: <
      - name: sorting
        in: query
        required: false
        description: Possibilities for sorting of the results.
        schema:
          type: string
          enum:
          - pValue
          - mscor
          - correlation
      - name: descending
        in: query
        required: false
        description: Descending (true) or ascending (false) ordering of the results.
        schema:
          type: boolean
          default: true
      - name: limit
        in: query
        description: Number of results that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
        required: false
        schema:
          type: integer
          default: 100
      - name: offset
        in: query
        description: Starting point from where results should be shown.
        required: false
        schema:
          type: integer
      - name: information
        in: query
        description: All available information about genes displayed (true) or just ensg number (false).
        required: false
        schema:
          type: boolean
          default: true
      responses:
        "200":
          description: Read all ceRNA interactions where gene of interesest in
            the searched disease (if provided) is involved in
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    correlation:
                      type: number
                      description: Correlation of the corresponding ceRNA interaction.
                    gene1:
                      type: object
                      description: One of the two interacting ceRNAs.
                      properties:
                        chromosome_name:
                          type: integer
                          description: Name of the chromosome the gene is located on.
                        description:
                          type: string
                          description: Anntotation from Ensemble of the gene.
                        end_pos:
                          type: integer
                          description: Endposition of the gene.
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                        gene_type:
                          type: string
                          description: The gene type includes both status (e.g. known) and biotype (e.g. protein coding).
                        start_pos:
                          type: integer
                          description: Starting position of the gene.
                    gene2:
                      type: object
                      description: The other part of the ceRNA interaction.
                      properties:
                        chromosome_name:
                          type: integer
                          description: Name of the chromosome the gene is located on.
                        description:
                          type: string
                          description: Anntotation from Ensemble of the gene.
                        end_pos:
                          type: integer
                          description: Endposition of the gene.
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                        gene_type:
                          type: string
                          description: The gene type includes both status (e.g. known) and biotype (e.g. protein coding).
                        start_pos:
                          type: integer
                          description: Starting position of the gene.
                    mscor:
                      type: number
                      description: Multiple senstivity correlation (mscor) of the corresponding ceRNA interaction.
                    p_value:
                      type: number
                      description: FDR adjusted p-value of the corresponding ceRNA interaction.
                    sponge_run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          properties:
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                            dataset_ID:
                                type: integer
                                description: Internal databse ID of the cancer type/dataset.
                            disease_name:
                                type: string
                                description: Name of the cancer type/dataset.
                          required:
                            - dataset_Id
                            - disease_name
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
  /ceRNAInteraction/findAllTranscripts:
    get:
      operationId: transcriptInteraction.read_all_transcripts
      tags:
        - ceRNANetwork
      summary: Get all ceRNA interactions the transcript(s) of interest is/are involed in.
      description: Get all ceRNA interactions by given identifications (enst_number or transcript_type), specific cancer type/dataset or different filter possibilities according different statistical values (e.g. FDR adjusted p-value).
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: dataset_ID
          in: query
          description: Internal database ID of the cancer type/dataset.
          required: false
          schema:
            type: integer
        - name: disease_name
          in: query
          description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
          required: false
          schema:
            type: string
        - name: enst_number
          in: query
          description: A comma-separated list of enst number(s) of interest (e.g. ENST00000369985).
          required: false
          schema:
            type: array
            items:
              type: string
            minItems: 0
          explode: false
          style: form
        - name: transcript_type
          in: query
          description: Defines the type of transcripts of interest (only one possibility choosable).
          required: false
          schema:
            type: string
            enum:
              - 3prime_overlapping_ncRNA
              - antisense
              - antisense_RNA
              - bidirectional_promoter_lncRNA
              - IG_C_gene
              - IG_C_pseudogene
              - IG_V_gene
              - IG_V_pseudogene
              - lincRNA
              - macro_lncRNA
              - miRNA
              - misc_RNA
              - Mt_rRNA
              - polymorphic_pseudogene
              - processed_pseudogene
              - processed_transcript
              - protein_coding
              - pseudogene
              - ribozyme
              - rRNA
              - rRNA_pseudogene
              - scaRNA
              - scRNA
              - sense_intronic
              - sense_overlapping
              - snoRNA
              - snRNA
              - TEC
              - TR_C_gene
              - TR_V_gene
              - TR_V_pseudogene
              - transcribed_processed_pseudogene
              - transcribed_unitary_pseudogene
              - transcribed_unprocessed_pseudogene
              - translated_processed_pseudogene
              - unitary_pseudogene
              - unprocessed_pseudogene
              - vaultRNA
        - name: pValue
          in: query
          required: false
          description: Threshold of the FDR adjusted p-value. Default is 0.05.
          schema:
            type: number
            default: 0.05
        - name: pValueDirection
          in: query
          required: false
          description: Direction of the FDR adjusted p-value threshold (<=, >=).
          schema:
            type: string
            enum:
              - <
              - ">"
            default: <
        - name: mscor
          in: query
          required: false
          description: Threshold of the 'multiple sensitivity correlation' (mscor).
          schema:
            type: number
        - name: mscorDirection
          in: query
          required: false
          description: Direction of the mscor threshold (<=, >=).
          schema:
            type: string
            enum:
              - <
              - ">"
            default: <
        - name: correlation
          in: query
          required: false
          description: Threshold of the correlation.
          schema:
            type: number
        - name: correlationDirection
          in: query
          required: false
          description: Direction of the correlation threshold (<=, >=).
          schema:
            type: string
            enum:
              - <
              - ">"
            default: <
        - name: sorting
          in: query
          required: false
          description: Possibilities for sorting of the results.
          schema:
            type: string
            enum:
              - pValue
              - mscor
              - correlation
        - name: descending
          in: query
          required: false
          description: Descending (true) or ascending (false) ordering of the results.
          schema:
            type: boolean
            default: true
        - name: limit
          in: query
          description: Number of results that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
          required: false
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          description: Starting point from where results should be shown.
          required: false
          schema:
            type: integer
        - name: information
          in: query
          description: All available information about genes displayed (true) or just enst number (false).
          required: false
          schema:
            type: boolean
            default: true
      responses:
        "200":
          description: Read all ceRNA interactions where transcript of interesest in
            the searched disease (if provided) is involved in
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    correlation:
                      type: number
                      description: Correlation of the corresponding ceRNA interaction.
                    transcript_1:
                      type: object
                      description: One of the two interacting ceRNAs.
                      properties:
                        gene:
                          type: object
                          description: gene the transcript belongs to
                          properties:
                            ensg_number:
                              type: string
                              description: Ensg number of the gene.
                            gene_symbol:
                              type: string
                              description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                            description:
                              type: string
                              description: Anntotation from Ensemble of the gene.
                        end_pos:
                          type: integer
                          description: Endposition of the trancript.
                        enst_number:
                          type: string
                          description: Ensg number of the gene.
                        transcript_type:
                          type: string
                          description: The transcript type includes both status (e.g. known) and biotype (e.g. protein coding).
                        start_pos:
                          type: integer
                          description: Starting position of the gene.
                        canonical_transcript:
                          type: integer
                          description: Is a canonical transcript
                    transcript_2:
                      type: object
                      description: The other part of the ceRNA interaction.
                      properties:
                        gene:
                          type: object
                          description: gene the transcript belongs to
                          properties:
                            ensg_number:
                              type: string
                              description: Ensg number of the gene.
                            gene_symbol:
                              type: string
                              description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                            description:
                              type: string
                              description: Anntotation from Ensemble of the gene.
                        end_pos:
                          type: integer
                          description: Endposition of the trancript.
                        enst_number:
                          type: string
                          description: Ensg number of the gene.
                        transcript_type:
                          type: string
                          description: The transcript type includes both status (e.g. known) and biotype (e.g. protein coding).
                        start_pos:
                          type: integer
                          description: Starting position of the gene.
                        canonical_transcript:
                          type: integer
                          description: Is a canonical transcript
                    mscor:
                      type: number
                      description: Multiple senstivity correlation (mscor) of the corresponding ceRNA interaction.
                    p_value:
                      type: number
                      description: FDR adjusted p-value of the corresponding ceRNA interaction.
                    sponge_run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          properties:
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                            dataset_ID:
                              type: integer
                              description: Internal databse ID of the cancer type/dataset.
                            disease_name:
                              type: string
                              description: Name of the cancer type/dataset.
                          required:
                            - dataset_Id
                            - disease_name
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
  /ceRNAInteraction/findSpecific:
    get:
      operationId: geneInteraction.read_specific_interaction
      tags:
      - ceRNANetwork
      summary: Get all interactions between the given identifiers (ensg_number or gene_symbol).
      description: Get all interactions between the given identifiers (ensg_number or gene_symbol).
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg number(s) of the gene(s) of interest(e.g. ENSG00000259090, ENSG00000217289, ENSG00000152284, ENSG00000185033) (this or gene_symbol is required)
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest(e.g. SEPT7P1,TIGAR, TCF7L1, SEMA4B) (this or ensg_number is required)
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
      - name: pValue
        in: query
        required: false
        description: Threshold of the FDR adjusted p-value. Default is 0.05.
        schema:
          type: number
          default: 0.05
      - name: pValueDirection
        in: query
        required: false
        description: Direction of the FDR adjusted p-value threshold (<=, >=).
        schema:
          type: string
          enum:
          - <
          - ">"
          default: <
      - name: limit
        in: query
        description: Number of results that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
        required: false
        schema:
          type: integer
          default: 100
      - name: offset
        in: query
        description: Starting point from where results should be shown.
        required: false
        schema:
          type: integer
      responses:
        "200":
          description: Get all interactions between the given identifiers (ensg_number or gene_symbol) - if existing.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    correlation:
                      type: number
                      description: Correlation of the corresponding ceRNA interaction.
                    gene1:
                      type: string
                      description: Ensg number of the first part of the ceRNA interaction.
                    gene2:
                      type: string
                      description: Ensg number of the second part of the ceRNA interaction.
                    mscor:
                      type: number
                      description: Multiple senstivity correlation (mscor) of the corresponding ceRNA interaction.
                    p_value:
                      type: number
                      description: FDR adjusted p-value of the corresponding ceRNA interaction.
                    run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          properties:
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                            dataset_ID:
                                type: integer
                                description: Internal databse ID of the cancer type/dataset.
                            disease_name:
                                type: string
                                description: Name of the cancer type/dataset.
                          required:
                            - dataset_ID
                            - disease_name
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
  /ceRNAInteraction/findSpecificTranscripts:
    get:
      operationId: transcriptInteraction.read_specific_interaction
      tags:
        - ceRNANetwork
      summary: Get all interactions between the given identifiers (enst_number).
      description: Get all interactions between the given identifiers (enst_number).
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: dataset_ID
          in: query
          description: Internal database ID of the cancer type/dataset.
          required: false
          schema:
            type: integer
        - name: disease_name
          in: query
          description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
          required: false
          schema:
            type: string
        - name: enst_number
          in: query
          description: A comma-separated list of enst number(s) of the transcripts(s) of interest(e.g. ENST00000259090)
          required: false
          schema:
            type: array
            items:
              type: string
            minItems: 0
          explode: false
          style: form
        - name: pValue
          in: query
          required: false
          description: Threshold of the FDR adjusted p-value. Default is 0.05.
          schema:
            type: number
            default: 0.05
        - name: pValueDirection
          in: query
          required: false
          description: Direction of the FDR adjusted p-value threshold (<=, >=).
          schema:
            type: string
            enum:
              - <
              - ">"
            default: <
        - name: limit
          in: query
          description: Number of results that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
          required: false
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          description: Starting point from where results should be shown.
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Get all interactions between the given identifiers (ensg_number or gene_symbol) - if existing.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    correlation:
                      type: number
                      description: Correlation of the corresponding ceRNA interaction.
                    transcript_1:
                      type: object
                      description: One of the two interacting ceRNAs.
                      properties:
                        gene:
                          type: object
                          description: gene the transcript belongs to
                          properties:
                            ensg_number:
                              type: string
                              description: Ensg number of the gene.
                            gene_symbol:
                              type: string
                              description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                            description:
                              type: string
                              description: Anntotation from Ensemble of the gene.
                        end_pos:
                          type: integer
                          description: Endposition of the trancript.
                        enst_number:
                          type: string
                          description: Ensg number of the gene.
                        transcript_type:
                          type: string
                          description: The transcript type includes both status (e.g. known) and biotype (e.g. protein coding).
                        start_pos:
                          type: integer
                          description: Starting position of the gene.
                        canonical_transcript:
                          type: integer
                          description: Is a canonical transcript
                    transcript_2:
                      type: object
                      description: The other part of the ceRNA interaction.
                      properties:
                        gene:
                          type: object
                          description: gene the transcript belongs to
                          properties:
                            ensg_number:
                              type: string
                              description: Ensg number of the gene.
                            gene_symbol:
                              type: string
                              description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                            description:
                              type: string
                              description: Anntotation from Ensemble of the gene.
                        end_pos:
                          type: integer
                          description: Endposition of the trancript.
                        enst_number:
                          type: string
                          description: Ensg number of the gene.
                        transcript_type:
                          type: string
                          description: The transcript type includes both status (e.g. known) and biotype (e.g. protein coding).
                        start_pos:
                          type: integer
                          description: Starting position of the gene.
                        canonical_transcript:
                          type: integer
                          description: Is a canonical transcript
                    mscor:
                      type: number
                      description: Multiple senstivity correlation (mscor) of the corresponding ceRNA interaction.
                    p_value:
                      type: number
                      description: FDR adjusted p-value of the corresponding ceRNA interaction.
                    sponge_run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          properties:
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                            dataset_ID:
                              type: integer
                              description: Internal databse ID of the cancer type/dataset.
                            disease_name:
                              type: string
                              description: Name of the cancer type/dataset.
                          required:
                            - dataset_ID
                            - disease_name
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
  /ceRNAInteraction/getGeneNetwork:
    get:
      operationId: geneInteraction.get_gene_network
      tags:
        - ceRNANetwork
      summary: Get all nodes and edges of the ceRNA network using various filters.
      description: Get all nodes and edges of the ceRNA network using all filters of both methods read_all_gene_network_analysis and read_specific_interaction together.
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: dataset_ID
          in: query
          description: Internal database ID of the cancer type/dataset.
          required: false
          schema:
            type: integer
        - name: disease_name
          in: query
          description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
          required: false
          schema:
            type: string
        - name: ensemblID
          in: query
          description: A comma-separated list of ensg number(s) of the gene(s) of interest(e.g. ENSG00000259090, ENSG00000217289, ENSG00000152284, ENSG00000185033)
          required: false
          schema:
            type: array
            items:
              type: string
          style: form
          explode: false
        - name: minBetweenness
          in: query
          description: Threshold of the betweenness.
          required: false
          schema:
            type: number
        - name: minNodeDegree
          in: query
          description: Threshold of the degree.
          required: false
          schema:
            type: number
        - name: minEigenvector
          in: query
          description: Threshold of the eigenvector.
          required: false
          schema:
            type: number
        - name: maxPValue
          in: query
          required: false
          description: Threshold of the FDR adjusted p-value. Default is 0.05.
          schema:
            type: number
            default: 0.05
        - name: minMscor
          in: query
          required: false
          description: Threshold of the 'multiple sensitivity correlation' (mscor).
          schema:
            type: number
        - name: minCorrelation
          in: query
          required: false
          description: Threshold of the correlation.
          schema:
            type: number
        - name: edgeSorting
          in: query
          required: false
          description: Possibilities for sorting of the edges.
          schema:
            type: string
            enum:
              - pValue
              - mscor
              - correlation
        - name: nodeSorting
          in: query
          required: false
          description: On or more metrics for sorting the nodes. Options are 'betweenness', 'node_degree', 'eigenvector'. Default is 'betweenness'. If multiple metrics are provided, the mean rank is used.
          schema:
            type: array
            items:
              type: string
            # enum:
            #   - betweenness
            #   - degree
            #   - eigenvector
        - name: maxNodes
          in: query
          description: Number of nodes that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
          required: false
          schema:
            type: integer
            default: 100
        - name: maxEdges
          in: query
          description: Number of edges that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
          required: false
          schema:
            type: integer
            default: 100
        - name: offsetNodes
          in: query
          description: Starting point from where nodes should be shown.
          required: false
          schema:
            type: integer
        - name: offsetEdges
          in: query
          description: Starting point from where edges should be shown.
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Successfully retrieved nodes and edges of the ceRNA network.
          content:
            application/json:
              schema:
                type: object
                properties:
                  gene_interactions:
                    type: array
                    items:
                      $ref: '#/components/schemas/GeneInteractionDatasetLongSchema'
                  network_analysis:
                    type: array
                    items:
                      $ref: '#/components/schemas/NetworkAnalysisSchema'
  /ceRNAInteraction/getTranscriptNetwork:
    get:
      operationId: transcriptInteraction.get_transcript_network
      tags:
        - ceRNANetwork
      summary: Get all nodes and edges of the ceRNA network using various filters.
      description: Get all nodes and edges of the ceRNA network using all filters of both methods read_all_gene_network_analysis and read_specific_interaction together.
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: dataset_ID
          in: query
          description: Internal database ID of the cancer type/dataset.
          required: false
          schema:
            type: integer
        - name: disease_name
          in: query
          description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
          required: false
          schema:
            type: string
        - name: ensemblID
          in: query
          description: A comma-separated list of enst number(s) of the transcript(s) of interest(e.g. ENST00000259090, ENST00000217289, ENST00000152284, ENST00000185033)
          required: false
          schema:
            type: array
            items:
              type: string
          style: form
          explode: false
        - name: minBetweenness
          in: query
          description: Threshold of the betweenness.
          required: false
          schema:
            type: number
        - name: minNodeDegree
          in: query
          description: Threshold of the degree.
          required: false
          schema:
            type: number
        - name: minEigenvector
          in: query
          description: Threshold of the eigenvector.
          required: false
          schema:
            type: number
        - name: maxPValue
          in: query
          required: false
          description: Threshold of the FDR adjusted p-value. Default is 0.05.
          schema:
            type: number
            default: 0.05
        - name: minMscor
          in: query
          required: false
          description: Threshold of the 'multiple sensitivity correlation' (mscor).
          schema:
            type: number
        - name: minCorrelation
          in: query
          required: false
          description: Threshold of the correlation.
          schema:
            type: number
        - name: edgeSorting
          in: query
          required: false
          description: Possibilities for sorting of the edges.
          schema:
            type: string
            enum:
              - pValue
              - mscor
              - correlation
        - name: nodeSorting
          in: query
          required: false
          description: Possibilities for sorting of the nodes.
          schema:
            type: array
            items:
              type: string
            # enum:
            #   - betweenness
            #   - degree
            #   - eigenvector
        - name: maxNodes
          in: query
          description: Number of nodes that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
          required: false
          schema:
            type: integer
            default: 100
        - name: maxEdges
          in: query
          description: Number of edges that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
          required: false
          schema:
            type: integer
            default: 100
        - name: offsetNodes
          in: query
          description: Starting point from where nodes should be shown.
          required: false
          schema:
            type: integer
        - name: offsetEdges
          in: query
          description: Starting point from where edges should be shown.
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Successfully retrieved nodes and edges of the ceRNA network.
          content:
            application/json:
              schema:
                type: object
                properties:
                  transcript_interactions:
                    type: array
                    items:
                      $ref: '#/components/schemas/TranscriptInteractionDatasetLongSchema'
                  network_analysis:
                    type: array
                    items:
                      $ref: '#/components/schemas/NetworkAnalysisSchemaTranscript'

                    
  /findceRNA:
    get:
      operationId: geneInteraction.read_all_gene_network_analysis
      tags:
      - ceRNANetwork
      summary: Get all ceRNAs in a disease of interest (search not for a specific ceRNA, but search for all ceRNAs satisfying filter functions)
      description: Get all ceRNAs in a disease of interest (search not for a specific ceRNA, but search for all ceRNAs satisfying filter functions)
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg number(s) of the gene(s) of interest(e.g. ENSG00000259090, ENSG00000217289) (this or gene_symbol is required)
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest(e.g. SEPT7P1,TIGAR) (this or ensg_number is required)
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
      - name: gene_type
        in: query
        description: Defines the type of gene of interest (only one possibility choosable).
        required: false
        schema:
          type: string
          enum:
          - 3prime_overlapping_ncRNA
          - antisense
          - antisense_RNA
          - bidirectional_promoter_lncRNA
          - IG_C_gene
          - IG_C_pseudogene
          - IG_V_gene
          - IG_V_pseudogene
          - lincRNA
          - macro_lncRNA
          - miRNA
          - misc_RNA
          - Mt_rRNA
          - polymorphic_pseudogene
          - processed_pseudogene
          - processed_transcript
          - protein_coding
          - pseudogene
          - ribozyme
          - rRNA
          - rRNA_pseudogene
          - scaRNA
          - scRNA
          - sense_intronic
          - sense_overlapping
          - snoRNA
          - snRNA
          - TEC
          - TR_C_gene
          - TR_V_gene
          - TR_V_pseudogene
          - transcribed_processed_pseudogene
          - transcribed_unitary_pseudogene
          - transcribed_unprocessed_pseudogene
          - translated_processed_pseudogene
          - unitary_pseudogene
          - unprocessed_pseudogene
          - vaultRNA
      - name: minBetweenness
        in: query
        description: Threshold of the betweenness.
        required: false
        schema:
          type: number
      - name: minNodeDegree
        in: query
        description: Threshold of the degree.
        required: false
        schema:
          type: number
      - name: minEigenvector
        in: query
        description: Threshold of the eigenvektor.
        required: false
        schema:
          type: number
      - name: sorting
        in: query
        required: false
        description: Possibilities for sorting of the results.
        schema:
          type: string
          enum:
          - betweenness
          - node_degree
          - eigenvector
      - name: descending
        in: query
        required: false
        description: Descending (true) or ascending (false) ordering of the results.
        schema:
          type: boolean
          default: true
      - name: limit
        in: query
        description: Number of results that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
        required: false
        schema:
          type: integer
          default: 100
      - name: offset
        in: query
        description: Starting point from where results should be shown.
        required: false
        schema:
          type: integer
      responses:
        "200":
          description: Read all ceRNAs in a disease of interest (search not for a specific ceRNA, but search for all ceRNAs satisfying filter functions).
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    betweenness:
                      type: number
                      description: Betweenness of the corresponding gene (considered as node) in the specific network.
                    eigenvector:
                      type: number
                      description: Eigenvector of the corresponding gene (considered as node) in the specific network.
                    gene:
                      type: object
                      description: Information about the gene that satisfies all filters.
                      properties:
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                      required:
                        - ensg_number
                        - gene_symbol
                    node_degree:
                      type: number
                      description: Degree of the corresponding gene (considered as node) in the specific network.
                    sponge_run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          properties:
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                            dataset_ID:
                              type: integer
                              description: Internal databse ID of the cancer type/dataset.
                            disease_name:
                              type: string
                              description: Name of the cancer type/dataset.
                          required:
                            - dataset_ID
                            - disease_name
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
  /findceRNATranscripts:
    get:
      operationId: transcriptInteraction.read_all_transcript_network_analysis
      tags:
        - ceRNANetwork
      summary: Get all ceRNAs in a disease of interest (search not for a specific ceRNA, but search for all ceRNAs satisfying filter functions)
      description: Get all ceRNAs in a disease of interest (search not for a specific ceRNA, but search for all ceRNAs satisfying filter functions)
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: dataset_ID
          in: query
          description: Internal database ID of the cancer type/dataset.
          required: false
          schema:
            type: integer
        - name: disease_name
          in: query
          description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
          required: false
          schema:
            type: string
        - name: enst_number
          in: query
          description: A comma-separated list of enst number(s) of the gene(s) of interest
          required: false
          schema:
            type: array
            items:
              type: string
            minItems: 0
          explode: false
          style: form
        - name: transcript_type
          in: query
          description: Defines the type of transcript of interest (only one possibility choosable).
          required: false
          schema:
            type: string
            enum:
              - 3prime_overlapping_ncRNA
              - antisense
              - antisense_RNA
              - bidirectional_promoter_lncRNA
              - IG_C_gene
              - IG_C_pseudogene
              - IG_V_gene
              - IG_V_pseudogene
              - lincRNA
              - macro_lncRNA
              - miRNA
              - misc_RNA
              - Mt_rRNA
              - polymorphic_pseudogene
              - processed_pseudogene
              - processed_transcript
              - protein_coding
              - pseudogene
              - ribozyme
              - rRNA
              - rRNA_pseudogene
              - scaRNA
              - scRNA
              - sense_intronic
              - sense_overlapping
              - snoRNA
              - snRNA
              - TEC
              - TR_C_gene
              - TR_V_gene
              - TR_V_pseudogene
              - transcribed_processed_pseudogene
              - transcribed_unitary_pseudogene
              - transcribed_unprocessed_pseudogene
              - translated_processed_pseudogene
              - unitary_pseudogene
              - unprocessed_pseudogene
              - vaultRNA
        - name: minBetweenness
          in: query
          description: Threshold of the betweenness.
          required: false
          schema:
            type: number
        - name: minNodeDegree
          in: query
          description: Threshold of the degree.
          required: false
          schema:
            type: number
        - name: minEigenvector
          in: query
          description: Threshold of the eigenvektor.
          required: false
          schema:
            type: number
        - name: sorting
          in: query
          required: false
          description: Possibilities for sorting of the results.
          schema:
            type: string
            enum:
              - betweenness
              - node_degree
              - eigenvector
        - name: descending
          in: query
          required: false
          description: Descending (true) or ascending (false) ordering of the results.
          schema:
            type: boolean
            default: true
        - name: limit
          in: query
          description: Number of results that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
          required: false
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          description: Starting point from where results should be shown.
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Read all ceRNAs in a disease of interest (search not for a specific ceRNA, but search for all ceRNAs satisfying filter functions).
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    betweenness:
                      type: number
                      description: Betweenness of the corresponding transcript (considered as node) in the specific network.
                    eigenvector:
                      type: number
                      description: Eigenvector of the corresponding transcript (considered as node) in the specific network.
                    transcript:
                      type: object
                      description: Information about the transcript that satisfies all filters.
                      properties:
                        enst_number:
                          type: string
                          description: Enst number of the transcript.
                        gene:
                          type: object
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                          properties:
                            ensg_number:
                              type: string
                              description: Ensg number of the gene that the transcript is part of
                            gene_symbol:
                              type: string
                              description: Symbol of the gene that the transcript is part of
                      required:
                        - enst_number
                    node_degree:
                      type: number
                      description: Degree of the corresponding transcript (considered as node) in the specific network.
                    sponge_run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          properties:
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                            dataset_ID:
                              type: integer
                              description: Internal databse ID of the cancer type/dataset.
                            disease_name:
                              type: string
                              description: Name of the cancer type/dataset.
                          required:
                            - dataset_ID
                            - disease_name
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
  /getGeneCount:
    get:
      operationId: geneInteraction.getGeneCounts
      tags:
      - ceRNANetwork
      summary: Get the number of times a gene of interest is involved in the complete network or just significant interactions.
      description: Get the number of times a gene of interest is involved in the complete network or just significant interactions.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg number(s) of the gene(s) of interest(e.g. ENSG00000259090, ENSG00000217289) (this or gene_symbol is required)
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest(e.g. SEPT7P1,TIGAR) (this or ensg_number is required)
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
      - name: minCountAll
        in: query
        description: Defines the minimal number of times a gene has to be involved in the complete network (e.g. the degree of the corresponding node must be greater than minCountAll).
        required: false
        schema:
          type: integer
      - name: minCountSign
        in: query
        description: Defines the minimal number of times a gene has to be involved in significant (p.adj < 0.05) interactions in the network.
        required: false
        schema:
          type: integer
      responses:
        "200":
          description: Get the number of times a gene of interest is involved in the complete network or just significant interactions.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    gene:
                      type: object
                      description: One of the two interacting ceRNAs.
                      properties:
                        ensg_number:
                          type: string
                          description: Ensg from Ensemble number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                      required:
                        - ensg_number
                        - gene_symbol
                    run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          description: Dataset ID and disease name.
                          properties: 
                            dataset_ID: 
                              type: integer
                              description: Internal database ID of the cancer type/dataset.
                            disease_name:
                              type: string
                              description: Name of the cancer type/dataset.
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
                    count_all:
                      description: Number of times gene is involved in the complete network (equals to degree).
                      type: string
                    count_sign:
                      description: Number of times gene is involved in significant (p.adj < 0.05) interactions of the netwrk.
                      type: string
  /getTranscriptCount:
    get:
      operationId: transcriptInteraction.getTranscriptCounts
      tags:
        - ceRNANetwork
      summary: Get the number of times a transcript of interest is involved in the complete network or just significant interactions.
      description: Get the number of times a transcript of interest is involved in the complete network or just significant interactions.
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: dataset_ID
          in: query
          description: Internal database ID of the cancer type/dataset.
          required: false
          schema:
            type: integer
        - name: disease_name
          in: query
          description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
          required: false
          schema:
            type: string
        - name: enst_number
          in: query
          description: A comma-separated list of enst number(s) of the transcript(s) of interest.
          required: false
          schema:
            type: array
            items:
              type: string
            minItems: 0
          explode: false
          style: form
        - name: minCountAll
          in: query
          description: Defines the minimal number of times a gene has to be involved in the complete network (e.g. the degree of the corresponding node must be greater than minCountAll).
          required: false
          schema:
            type: integer
        - name: minCountSign
          in: query
          description: Defines the minimal number of times a gene has to be involved in significant (p.adj < 0.05) interactions in the network.
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Get the number of times a gene of interest is involved in the complete network or just significant interactions.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    transcript:
                      type: object
                      description: A transcript.
                      properties:
                        enst_number:
                          type: string
                          description: Enst from Ensemble number of the transcript.
                    count_all:
                      description: Number of times the transcript is involved in the complete network (equals to degree).
                      type: string
                    count_sign:
                      description: Number of times the transcript is involved in significant (p.adj < 0.05) interactions of the netwrk.
                      type: string
  /miRNAInteraction/findSpecific:
    get:
      operationId: geneInteraction.read_all_to_one_mirna
      tags:
      - ceRNANetwork
      summary: Get all ceRNA interactions where miRNA of interest contribute to.
      description: Get all ceRNA interactions where miRNA(s) of interest (different identifiers available - e.g. hs number or mimat number) contribute to.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: mimat_number
        in: query
        description: The mimat_id of the miRNA of interest (e.g. MIMAT0000076) - this or the hs_number is needed.
        required: false
        schema:
          type: string
      - name: hs_number
        in: query
        description: The hs_number of the miRNA of interest (e.g. hsa-miR-21-5p) - this or the mimat_numbers is needed.
        required: false
        schema:
          type: string
      - name: pValue
        in: query
        required: false
        description: Threshold of the FDR adjusted p-value. Default is 0.05.
        schema:
          type: number
          default: 0.05
      - name: pValueDirection
        in: query
        required: false
        description: Direction of the FDR adjusted p-value threshold (<=, >=).
        schema:
          type: string
          enum:
            - <
            - ">"
          default: <
      - name: mscor
        in: query
        required: false
        description: Threshold of the 'multiple sensitivity correlation' (mscor).
        schema:
          type: number
      - name: mscorDirection
        in: query
        required: false
        description: Direction of the mscor threshold (<=, >=).
        schema:
          type: string
          enum:
            - <
            - ">"
          default: <
      - name: correlation
        in: query
        required: false
        description: Threshold of the correlation.
        schema:
          type: number
      - name: correlationDirection
        in: query
        required: false
        description: Direction of the correlation threshold (<=, >=).
        schema:
          type: string
          enum:
            - <
            - ">"
          default: <
      - name: limit
        in: query
        description: Number of results that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
        required: false
        schema:
          type: integer
          default: 100
      - name: offset
        in: query
        description: Starting point from where results should be shown.
        required: false
        schema:
          type: integer
      responses:
        "200":
          description: Get all interactions between the given identifiers (ensg_number or gene_symbol) - if existing.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    correlation:
                      type: number
                      description: Correlation of the corresponding ceRNA interaction.
                    gene1:
                      type: string
                      description: Ensg number of the first part of the ceRNA interaction.
                    gene2:
                      type: string
                      description: Ensg number of the second part of the ceRNA interaction.
                    mscor:
                      type: number
                      description: Multiple senstivity correlation (mscor) of the corresponding ceRNA interaction.
                    p_value:
                      type: number
                      description: FDR adjusted p-value of the corresponding ceRNA interaction.
                    run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          properties:
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                            dataset_ID:
                              type: integer
                              description: Internal databse ID of the cancer type/dataset.
                            disease_name:
                              type: string
                              description: Name of the cancer type/dataset.
                          required:
                          - dataset_ID
                          - disease_name
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                      - dataset
                      - sponge_run_ID
  /miRNAInteraction/findSpecificTranscripts:
    get:
      operationId: transcriptInteraction.read_all_to_one_mirna
      tags:
        - ceRNANetwork
      summary: Get all ceRNA interactions where miRNA of interest contribute to.
      description: Get all ceRNA interactions where miRNA(s) of interest (different identifiers available - e.g. hs number or mimat number) contribute to.
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: disease_name
          in: query
          description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
          required: false
          schema:
            type: string
        - name: mimat_number
          in: query
          description: The mimat_id of the miRNA of interest (e.g. MIMAT0000076) - this or the hs_number is needed.
          required: false
          schema:
            type: string
        - name: hs_number
          in: query
          description: The hs_number of the miRNA of interest (e.g. hsa-miR-21-5p) - this or the mimat_numbers is needed.
          required: false
          schema:
            type: string
        - name: pValue
          in: query
          required: false
          description: Threshold of the FDR adjusted p-value. Default is 0.05.
          schema:
            type: number
            default: 0.05
        - name: pValueDirection
          in: query
          required: false
          description: Direction of the FDR adjusted p-value threshold (<=, >=).
          schema:
            type: string
            enum:
              - <
              - ">"
            default: <
        - name: mscor
          in: query
          required: false
          description: Threshold of the 'multiple sensitivity correlation' (mscor).
          schema:
            type: number
        - name: mscorDirection
          in: query
          required: false
          description: Direction of the mscor threshold (<=, >=).
          schema:
            type: string
            enum:
              - <
              - ">"
            default: <
        - name: correlation
          in: query
          required: false
          description: Threshold of the correlation.
          schema:
            type: number
        - name: correlationDirection
          in: query
          required: false
          description: Direction of the correlation threshold (<=, >=).
          schema:
            type: string
            enum:
              - <
              - ">"
            default: <
        - name: limit
          in: query
          description: Number of results that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
          required: false
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          description: Starting point from where results should be shown.
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Get all interactions between the given identifiers (ensg_number or gene_symbol) - if existing.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    correlation:
                      type: number
                      description: Correlation of the corresponding ceRNA interaction.
                    transcript_1:
                      type: object
                      description: transcript of the first part of the ceRNA interaction.
                      properties:
                        enst_number:
                          type: string
                          description: Ensg from Ensemble number of the gene.
                        gene:
                          type: object
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                          properties:
                            ensg_number:
                              type: string
                              description: Ensg number of the gene that the transcript is part of
                            gene_symbol:
                              type: string
                              description: Symbol of the gene that the transcript is part of
                    transcript_2:
                      type: object
                      description: transcript of the second part of the ceRNA interaction.
                      properties:
                        enst_number:
                          type: string
                          description: Ensg from Ensemble number of the gene.
                        gene:
                          type: object
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                          properties:
                            ensg_number:
                              type: string
                              description: Ensg number of the gene that the transcript is part of
                            gene_symbol:
                              type: string
                              description: Symbol of the gene that the transcript is part of
                    mscor:
                      type: number
                      description: Multiple senstivity correlation (mscor) of the corresponding ceRNA interaction.
                    p_value:
                      type: number
                      description: FDR adjusted p-value of the corresponding ceRNA interaction.
                    sponge_run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          properties:
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                            dataset_ID:
                              type: integer
                              description: Internal databse ID of the cancer type/dataset.
                            disease_name:
                              type: string
                              description: Name of the cancer type/dataset.
                          required:
                            - dataset_ID
                            - disease_name
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
  /miRNAInteraction/getOccurence:
    get:
      operationId: geneInteraction.read_all_mirna
      tags:
      - ceRNANetwork
      summary: Get all mirna involved in cancer type/dataset of interest (searchs not for a specific miRNA, but search for all miRNA satisfying filter functions).
      description: Get all mirna involved in cancer type/dataset of interest (searchs not for a specific miRNA, but search for all miRNA satisfying filter functions).
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        schema:
          type: string
      - name: mimat_number
        in: query
        description: A comma-separated list of mimat_id(s) of the miRNA(s) of interest (e.g. MIMAT0000076, MIMAT0000261) (this or the hs_number(s) is/are needed).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: hs_number
        in: query
        description: A comma-separated list of hs_number(s) of the miRNA(s) of interest (e.g. hsa-miR-21-5p, hsa-miR-183-5p) (this or the mimat_number(s) is/are needed).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: occurences
        in: query
        description: Threshold of amount of contributions a miRNA should have to be considered.
        required: false
        schema:
          type: integer
      - name: sorting
        in: query
        required: false
        description: Possibilities for sorting of the results.
        schema:
          type: string
          enum:
          - occurences
      - name: descending
        in: query
        required: false
        description: Descending (true) or ascending (false) ordering of the results.
        schema:
          type: boolean
          default: true
      - name: limit
        in: query
        description: Number of results that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
        required: false
        schema:
          type: integer
          default: 100
      - name: offset
        in: query
        description: Starting point from where results should be shown.
        required: false
        schema:
          type: integer
      responses:
        "200":
          description: Read all ceRNA interactions where miRNA of interesest in
            the searched disease (if provided) contributes to.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    mirna:
                      type: object
                      description: Information about the miRNA(s) of interest - source is miRBase
                      properties:
                        hs_nr:
                          type: string
                          description: The names/identifiers in the database are of the form hsa-mir-121. The first three letters signify the organism. The mature miRNA is designated miR-121 in the database.
                        mir_ID:
                          type: string
                          description: The accession number is the only truly stable identifier for an entry -- miRNA names may change from those published as relationships between sequences become clear.
                      required:
                        - hs_nr
                        - mir_ID
                    occurences:
                      type: integer
                      description: Occurences of the miRNA.
                    run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          properties:
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                            dataset_ID:
                              type: integer
                              description: Internal databse ID of the cancer type/dataset.
                            disease_name:
                              type: string
                              description: Name of the cancer type/dataset.
                          required:
                            - dataset_ID
                            - disease_name
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
  /miRNAInteraction/getOccurenceTranscripts:
    get:
      operationId: transcriptInteraction.read_all_mirna
      tags:
        - ceRNANetwork
      summary: Get all mirna involved in cancer type/dataset of interest (searchs not for a specific miRNA, but search for all miRNA satisfying filter functions).
      description: Get all mirna involved in cancer type/dataset of interest (searchs not for a specific miRNA, but search for all miRNA satisfying filter functions).
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: disease_name
          in: query
          description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
          schema:
            type: string
        - name: mimat_number
          in: query
          description: A comma-separated list of mimat_id(s) of the miRNA(s) of interest (e.g. MIMAT0000076, MIMAT0000261) (this or the hs_number(s) is/are needed).
          required: false
          schema:
            type: array
            items:
              type: string
            minItems: 0
          explode: false
          style: form
        - name: hs_number
          in: query
          description: A comma-separated list of hs_number(s) of the miRNA(s) of interest (e.g. hsa-miR-21-5p, hsa-miR-183-5p) (this or the mimat_number(s) is/are needed).
          required: false
          schema:
            type: array
            items:
              type: string
            minItems: 0
          explode: false
          style: form
        - name: occurences
          in: query
          description: Threshold of amount of contributions a miRNA should have to be considered.
          required: false
          schema:
            type: integer
        - name: sorting
          in: query
          required: false
          description: Possibilities for sorting of the results.
          schema:
            type: string
            enum:
              - occurences
        - name: descending
          in: query
          required: false
          description: Descending (true) or ascending (false) ordering of the results.
          schema:
            type: boolean
            default: true
        - name: limit
          in: query
          description: Number of results that should be shown. Default value is 100 and can be up to 1000. For more results please use batches, the provided offset parameter or download the whole dataset.
          required: false
          schema:
            type: integer
            default: 100
        - name: offset
          in: query
          description: Starting point from where results should be shown.
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Read all ceRNA interactions where miRNA of interesest in
            the searched disease (if provided) contributes to.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    mirna:
                      type: object
                      description: Information about the miRNA(s) of interest - source is miRBase
                      properties:
                        hs_nr:
                          type: string
                          description: The names/identifiers in the database are of the form hsa-mir-121. The first three letters signify the organism. The mature miRNA is designated miR-121 in the database.
                        mir_ID:
                          type: string
                          description: The accession number is the only truly stable identifier for an entry -- miRNA names may change from those published as relationships between sequences become clear.
                      required:
                        - hs_nr
                        - mir_ID
                    occurences:
                      type: integer
                      description: Occurences of the miRNA.
                    sponge_run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          properties:
                            data_origin:
                              type: string
                              description: Origin of the dataset.
                            dataset_ID:
                              type: integer
                              description: Internal databse ID of the cancer type/dataset.
                            disease_name:
                              type: string
                              description: Name of the cancer type/dataset.
                          required:
                            - dataset_ID
                            - disease_name
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
  /miRNAInteraction/findceRNA:
    get:
      operationId: geneInteraction.read_mirna_for_specific_interaction
      tags:
        - ceRNANetwork
      description: Get all miRNAs that contribute to all interactions between the given identifiers (ensg_number or gene_symbol).
      summary: Get all miRNAs that contribute to all interactions between the given identifiers (ensg_number or gene_symbol).
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg number(s) of the gene(s) of interest(e.g. ENSG00000152284, ENSG00000185033) (this or gene_symbol is required).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest(e.g. SEPT7P1,TIGAR, TCF7L1, SEMA4B) (this or ensg_number is required).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: between
        in: query
        description: If false, all interactions where one of the interaction partners fits the given genes of interes twill be considered. If true, just interactions between the genes of interest will be considered.
        required: true
        schema:
          type: boolean
          default: true
      responses:
        "200":
          description: Returns all interactions between the given identifications (ensg_number or gene_symbol)
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    gene:
                      type: object
                      description: Gene of interest.
                      properties:
                        ensg_number:
                          type: string
                          description: Ensg from Ensemble number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                      required:
                        - ensg_number
                        - gene_symbol
                    sponge_run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          description: Dataset ID and disease name.
                          properties: 
                            dataset_ID: 
                              type: integer
                              description: Internal database ID of the cancer type/dataset.
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
                    mirna:
                      type: object
                      description: Information about the miRNA(s) of interest - source is miRBase
                      properties:
                        hs_nr:
                          type: string
                          description: The names/identifiers in the database are of the form hsa-mir-121. The first three letters signify the organism. The mature miRNA is designated miR-121 in the database.
                        mir_ID:
                          type: string
                          description: The accession number is the only truly stable identifier for an entry -- miRNA names may change from those published as relationships between sequences become clear.
                      required:
                        - hs_nr
                        - mir_ID
                    coefficient:
                      type: number
                      description: Coefficient of the corresponding ceRNA interaction.
  /miRNAInteraction/findceRNATranscripts:
    get:
      operationId: transcriptInteraction.read_mirna_for_specific_interaction
      tags:
        - ceRNANetwork
      description: Get all miRNAs that contribute to all interactions between the given identifiers (enst_number).
      summary: Get all miRNAs that contribute to all interactions between the given identifiers (enst_number).
      parameters:
        - $ref: '#/components/parameters/VersionParam'
        - name: dataset_ID
          in: query
          description: Internal database ID of the cancer type/dataset.
          required: false
          schema:
            type: integer
        - name: disease_name
          in: query
          description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
          required: false
          schema:
            type: string
        - name: enst_number
          in: query
          description: A comma-separated list of enst number(s) of the transcript(s) of interest(e.g. ENST00000225538, ENST00000245907).
          required: false
          schema:
            type: array
            items:
              type: string
            minItems: 0
          explode: false
          style: form
        - name: between
          in: query
          description: If false, all interactions where one of the interaction partners fits the given genes of interestwill be considered.If true, just interactions between the genes of interest will be considered.
          required: true
          schema:
            type: boolean
            default: true
      responses:
        "200":
          description: Returns all interactions between the given identifications (ensg_number or gene_symbol)
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    transcript:
                      type: object
                      description: Transcript of interest.
                      properties:
                        enst_number:
                          type: string
                          description: Ensg from Ensemble number of the gene.
                        gene:
                          type: object
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                          properties:
                            ensg_number:
                              type: string
                              description: Ensg number of the gene that the transcript is part of
                            gene_symbol:
                              type: string
                              description: Symbol of the gene that the transcript is part of
                      required:
                        - enst_number
                    sponge_run:
                      description: Information about specific run and corresponding cancer type/dataset.
                      type: object
                      properties:
                        dataset:
                          type: object
                          description: Dataset ID and disease name.
                          properties: 
                            dataset_ID: 
                              type: integer
                              description: Internal database ID of the cancer type/dataset.
                        sponge_run_ID:
                          type: integer
                          description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                      required:
                        - dataset
                        - sponge_run_ID
                    mirna:
                      type: object
                      description: Information about the miRNA(s) of interest - source is miRBase
                      properties:
                        hs_nr:
                          type: string
                          description: The names/identifiers in the database are of the form hsa-mir-121. The first three letters signify the organism. The mature miRNA is designated miR-121 in the database.
                        mir_ID:
                          type: string
                          description: The accession number is the only truly stable identifier for an entry -- miRNA names may change from those published as relationships between sequences become clear.
                      required:
                        - hs_nr
                        - mir_ID
  /exprValue/getceRNA:
    get:
      operationId: expressionValues.get_gene_expr
      tags:
      - ExpressionValues
      summary: Get all expression values for gene(s) of interest.
      description: Get all expression values for gene(s) of interest.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg numbers of the gene(s) of interest (e.g. ENSG00000259090, ENSG00000217289).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest (e.g. SEPT7P1,TIGAR).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: limit
        in: query
        description: Number of results that should be shown.
        required: false
        schema:
          type: integer
      - name: offset
        in: query
        description: Starting point from where results should be shown.
        required: false
        schema:
          type: integer
      - name: cluster
        in: query
        description: If true, the results will be clustered by gene and sample.
        required: false
        schema:
          type: boolean
      responses:
        "200":
          description: Get all expression values for gene(s) of interest.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    dataset:
                      type: object
                      description: Dataset ID and disease name.
                      properties: 
                        dataset_ID: 
                          type: integer
                          description: Internal database ID of the cancer type/dataset.
                        disease_name: 
                          type: string
                          description: Name of the cancer type.
                    expr_value:
                      type: number
                      description: Specific expression value for unique gene and sample_ID.
                    gene:
                      type: object
                      description: Information about the gene.
                      properties:
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                      required:
                      - ensg_number
                      - gene_symbol
                    sample_ID:
                      type: string
                      description: ID of the specific sample.
  /exprValue/getTranscriptExpr:
    get:
      operationId: expressionValues.get_transcript_expression
      tags:
      - ExpressionValues
      summary: Get transcript expressions for transcript IDs, gene IDs, or gene symbols
      description: Get transcript expressions for transcript IDs, gene IDs, or gene symbols
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the disease, will return all expression data if not specified
        required: false
        schema:
          type: string
      - name: enst_number
        in: query
        description: A comma-separated list of enst numbers of the transcript(s) of interest (e.g. ENST00000225538, ENST00000245907).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg numbers of the gene(s) of interest (e.g. ENSG00000259090, ENSG00000217289).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest (e.g. SEPT7P1,TIGAR).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: limit
        in: query
        description: Number of results that should be shown.
        required: false
        schema:
          type: integer
      - name: offset
        in: query
        description: Starting point from where results should be shown.
        required: false
        schema:
          type: integer
      - name: cluster
        in: query
        description: If true, the results will be clustered by gene and sample.
        required: false
        schema:
          type: boolean
      responses:
        "200":
          description: Expression values for all transcripts that fit the provided filters
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    dataset:
                      type: object
                      description: Dataset ID and disease name.
                      properties: 
                        dataset_ID: 
                          type: integer
                          description: Internal database ID of the cancer type/dataset.
                        disease_name: 
                          type: string
                          description: Name of the cancer type.
                    transcript:
                      type: string
                      description: Ensembl transcript ID
                    expr_value:
                      type: number
                      description: Specific expression value for unique transcript and sample_ID.
                    gene:
                      type: object
                      description: Information about the gene.
                      properties:
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                    sample_ID:
                      type: string
                      description: ID of the specific sample.
  /exprValue/getmiRNA:
    get:
      operationId: expressionValues.get_mirna_expr
      tags:
      - ExpressionValues
      summary: Get all expression values for miRNA(s) of interest.
      description: Get all expression values for miRNA(s) of interest.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: mimat_number
        in: query
        description: mimat_numbers(s) of the miRNA(s) of interest (e.g. MIMAT0000076, MIMAT0000261)
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: hs_number
        in: query
        description: A comma-separated list of hs_number(s) of the miRNA(s) of interest (e.g. hsa-miR-21-5p, hsa-miR-183-5p).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Get all expression values for ceRNA of interest
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    dataset:
                      type: object
                      description: Dataset ID and disease name.
                      properties: 
                        dataset_ID: 
                          type: integer
                          description: Internal database ID of the cancer type/dataset.
                        disease_name: 
                          type: string
                          description: Name of the cancer type.
                    expr_value:
                      type: number
                      description: Specific expression value for unique gene and sample_ID.
                    mirna:
                      type: object
                      description: Information about the miRNA.
                      properties:
                        hs_nr:
                          type: string
                          description: The names/identifiers in the database are of the form hsa-mir-121. The first three letters signify the organism. The mature miRNA is designated miR-121 in the database.
                        mir_ID:
                          type: string
                          description: The accession number is the only truly stable identifier for an entry -- miRNA names may change from those published as relationships between sequences become clear.
                      required:
                        - hs_nr
                        - mir_ID
                    sample_ID:
                      type: string
                      description: ID of the specific sample.
  /stringSearch:
    get:
      operationId: externalInformation.getAutocomplete
      tags:
      - ExternalInformation
      summary: Retrieve all possible matching ensg numbers, gene-symbols, hs-numbers or mimat numbers for the search string (mainly used for autocomplete function on the SPONGE webside)
      description: Retrieve all possible matching ensg numbers, gene-symbols, hs-numbers or mimat numbers for the search string (mainly used for autocomplete function on the SPONGE webside)
      parameters:
      - name: searchString
        in: query
        description: Prefix of a gene name/ensg number or mirna identification
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Retrieve all possible matchings
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    autocompleteSuggestion:
                      type: string
                      description: Possible autocompletion for the searchString
  /stringSearchTranscript: 
    get:
      operationId: externalInformation.getAutocompleteTranscripts
      tags:
      - ExternalInformation
      summary: Retrieve all possible matching enst numbers or gene-symbols for the search string (mainly used for autocomplete function on the SPONGE webside)
      description: Retrieve all possible matching enst numbers or gene-symbols for the search string (mainly used for autocomplete function on the SPONGE webside)
      parameters:
      - name: searchString
        in: query
        description: Prefix of a transcript enst number or a gene 
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Retrieve all possible matchings
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    autocompleteSuggestion:
                      type: object
                      description: Possible autocompletion for the searchString
                      properties:
                        enst_number:
                          type: string
                          description: Ensembl transcript ID
                        gene:
                          type: object
                          description: Gene of interest.
                          properties:
                            ensg_number:
                              type: string
                              description: Ensg from Ensemble number of the gene.
                            gene_symbol:
                              type: string
                              description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
  /getGeneInformation:
    get:
      operationId: externalInformation.getGeneInformation
      tags:
        - ExternalInformation
      summary: Get all available information about the gene of iterest.
      description: Convert ensg_number in gene_symbol or the way around. Be aware, that not all genes have a gene symbol provided.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg numbers of the gene(s) of interest (e.g. ENSG00000259090, ENSG00000217289).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest (e.g. SEPT7P1,TIGAR).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Get all available information about the gene of iterest.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    chromosome_name:
                      type: integer
                      description: Name of the chromosome the gene is located on.
                    description:
                      type: string
                      description: Anntotation from Ensemble of the gene.
                    end_pos:
                      type: integer
                      description: Endposition of the gene.
                    ensg_number:
                      type: string
                      description: Ensg number of the gene.
                    gene_symbol:
                      type: string
                      description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                    gene_type:
                      type: string
                      description: The gene type includes both status (e.g. known) and biotype (e.g. protein coding).
                    start_pos:
                      type: integer
                      description: Starting position of the gene.
                    cytoband:
                      type: string
                      description: Cytoband / Karyotype band of the gene.
                    strand:
                      type: integer
                      description: Type of strand of the gene (1 or -1).
  /getTranscriptInformation:
    get:
      operationId: externalInformation.getTranscriptInformation
      tags:
        - ExternalInformation
      summary: Get all available information about the transcript of interest.
      description: Get all available information about the transcript of interest.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: enst_number
        in: query
        description: A comma-separated list of enst_number of the transcript(s) of interest (e.g. ENST00000387314, ENST00000389680)
        required: true
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Get all available information about the transcript of interest.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    gene:
                      type: object
                      description: Information about the gene.
                      properties:
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                    enst_number:
                      type: string
                      description: Ensembl transcript ID
                    transcript_type:
                      type: string
                      description: Type of transcript.
                    start_pos:
                      type: integer
                      description: Startposition of transcript.
                    end_pos:
                      type: integer
                      description: Endposition of transcript.
                    canonical_transcript:
                      type: integer
                      description: Boolean if transcript is canonical (True = 1, False = 0)
  /getOverallCounts:
    get:
      operationId: externalInformation.getOverallCount
      tags:
      - ExternalInformation
      summary: Function return current statistic about database - amount of shared miRNA, significant and insignificant interactions per dataset.
      description: Function return current statistic about database - amount of shared miRNA, significant and insignificant interactions per dataset.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: level
        in: query
        description: Level of the statistic that should be returned. If not provided, all levels will be returned.
        required: false
        schema:
          type: string
          enum:
          - gene
          - transcript
      responses:
        "200":
          description: Function return current statistic about database - amount of shared miRNA, significant and insignificant interactions per dataset.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    count_interactions:
                      type: integer
                      description: Amount of all interactions in the specific dataset.
                    count_interactions_sign:
                      type: integer
                      description: Amount of all significant interactions in the specific dataset.
                    count_shared_miRNAs:
                      type: integer
                      description: Amount of shared miRNA  in the specific dataset.
                    sponge_run_ID:
                      type: integer
                      description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                    disease_name:
                      type: string
                      description: Name of the cancer type/dataset.
                    disease_subtype:
                      type: string
                      description: Name of the disease subtype.
  /getGeneOntology:
    get:
      operationId: externalInformation.getGeneOntology
      tags:
      - ExternalInformation
      summary: Function return associated GO Terms for gene(s) of interest.
      description: Function return associated GO Terms for gene(s) of interest. QuickGO - a fast web-based browser of the Gene Ontology and Gene Ontology annotation data - is used as external source.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest (e.g. PTEN,TIGAR).
        required: true
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Function return associated GO Terms for gene(s) of interest. QuickGO - a fast web-based browser of the Gene Ontology and Gene Ontology annotation data - is used as external source.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    gene:
                      type: object
                      description: Information about the gene.
                      properties:
                        description:
                          type: string
                          description: Biological descriotion of the GO Term of the specific gene symbol.
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                        gene_ontology_symbol:
                          type: string
                          description: GO Term of the specific gene symbol.
  /getHallmark:
    get:
      operationId: externalInformation.getHallmark
      tags:
      - ExternalInformation
      summary: Function return associated cancer hallmark for gene(s) of interest.
      description: Function return associated cancer hallmark for gene(s) of interest. Cancer Hallmark Genes (http://bio-bigdata.hrbmu.edu.cn/CHG/) is used as external source.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest (e.g. TUBBP2,CSNK1A1L).
        required: true
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Function return associated cancer hallmark for gene(s) of interest. Cancer Hallmark Genes (http://bio-bigdata.hrbmu.edu.cn/CHG/) is used as external source.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    gene:
                      type: object
                      description: Information about the gene.
                      properties:
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                        hallmark:
                          type: string
                          description: One of the 11 hallmarks of cancer.
  /getWikipathway:
    get:
      operationId: externalInformation.getWikipathway
      tags:
      - ExternalInformation
      summary: Function return associated wikipathway keys for gene(s) of interest.
      description: Function return associated wikipathway keys for gene(s) of interest.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest (e.g. PTEN,GJA1).
        required: true
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Function return associated cancer hallmark for gene(s) of interest.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    gene:
                      type: object
                      description: Information about the gene.
                      properties:
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                        wp_key:
                          type: string
                          description: Wikipathway key for gene(s) of interest.
  /getTranscriptGene:
    get:
      operationId: externalInformation.getTranscriptGene
      tags:
      - ExternalInformation
      summary: Function returns gene ID(s) of transcript(s) of interest.
      description: Function returns gene ID(s) of transcript(s) of interest.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: enst_number
        in: query
        description: A comma-separated list pf enst numbers of the transcript(s) of interest (e.g. ENST00000387314, ENST00000389680)
        required: true
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Function returns gene ID(s) for transcript(s) of interest
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    ensg_number:
                      type: string
                      description: Gene number of the transcript
                    gene_symbol:
                      type: string
                      description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
  /getGeneTranscripts:
    get:
      operationId: externalInformation.getGeneTranscripts
      tags:
      - ExternalInformation
      summary: Get all included transcripts of a gene of interest.
      description: Get all included transcripts of a gene of interest.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg numbers of the gene(s) of interest (e.g. ENSG00000259090, ENSG00000217289).
        required: true
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Successfully retrieved transcript IDs for the given gene IDs.
          content:
              application/json:
                schema:
                  type: array
                  items:
                    type: array
                    items:
                      properties:
                        enst_number:
                          type: string
                          description: Ensembl transcript ID
  /survivalAnalysis/sampleInformation:
    get:
      operationId: survivalAnalysis.get_patient_information
      tags:
        - SurvivalAnalysis
      summary: Get all available clinical information for patients/samples.
      description: Get all available clinical information for patients/samples.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_subtype
        in: query
        description: name of the disease subtype to get (e.g. "Her2" or just "Her")
        required: false
        schema:
          type: string
      - name: sample_ID
        in: query
        description: A comma-separated list of sample_ID of the patient/sample of interest (e.g. TCGA-BP-4968, TCGA-B8-A54F).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Get all available clinical information for patients/samples.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    dataset:
                      type: object
                      description: Dataset ID and disease name.
                      properties: 
                        dataset_ID: 
                          type: integer
                          description: Internal database ID of the cancer type/dataset.
                        disease_name: 
                          type: string
                          description: Name of the cancer type.
                    disease_status:
                      type: integer
                      description: Information about the disease status (0 = patient is dead, 1 = patient is alive) at the end of observation.
                    sample_ID:
                      type: string
                      description: Unique ID for sample/patient.
                    survival_time:
                      type: integer
                      description: Time of survival in days.
                    disease_ID: 
                      type: integer
                      description: Internal database ID of the disease of this patient. 
  /survivalAnalysis/getRates:
    get:
      operationId: survivalAnalysis.get_survival_rate
      tags:
        - SurvivalAnalysis
      summary: Get all raw data for kaplan meier plots.
      description: Get all raw data for kaplan meier plots.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_subtype
        in: query
        description: name of the disease subtype to get (e.g. "Her2" or just "Her")
        required: false
        schema:
          type: string
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg numbers of the gene(s) of interest (e.g. ENSG00000259090, ENSG00000217289).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest (e.g. SEPT7P1,TIGAR).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: sample_ID
        in: query
        description: A comma-separated list of sample_ID of the patient/sample of interest (e.g. TCGA-BP-4968, TCGA-B8-A54F).
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Get all raw data for kaplan meier plots.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    dataset:
                      type: object
                      description: Dataset ID and disease name.
                      properties: 
                        dataset_ID: 
                          type: integer
                          description: Internal database ID of the cancer type/dataset.
                        disease_name: 
                          type: string
                          description: Name of the cancer type.
                    gene:
                      type: object
                      description: Information about the gene.
                      properties:
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                      required:
                      - ensg_number
                      - gene_symbol
                    overexpression:
                      type: integer
                      description: Information about expression value of the gene (0 = underexpression, gene expression <= mean gene expression over all samples, 1 = overexpression, gene expression >= mean gene expression over all samples)
                    patient_information:
                      type: object
                      description: Information about the sample/patient.
                      properties:
                        disease_status:
                          type: integer
                          description: Information about the disease status (0 = patient is dead, 1 = patient is alive) at the end of observation.
                        sample_ID:
                          type: string
                          description: Unique ID for sample/patient.
                        survival_time:
                          type: integer
                          description: Time of survival in days.
  /survivalAnalysis/getPValues:
    get:
      operationId: survivalAnalysis.get_survival_pValue
      tags:
      - SurvivalAnalysis
      summary: Retrieve pValues from log rank test based on raw survival analysis data
      description: Retrieve pValues from log rank test based on raw survival analysis data
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: name of the disease to get (e.g. "kidney clear cell carcinoma" or just "kidney")
        required: false
        schema:
          type: string
      - name: disease_subtype
        in: query
        description: name of the disease subtype to get (e.g. "Her2" or just "Her")
        required: false
        schema:
          type: string
      - name: ensg_number
        in: query
        description: A comma-separated list of ensg numbers of the gene(s) of interest(e.g. ENSG00000259090, ENSG00000217289)
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: A comma-separated list of gene symbol(s) of the gene of interest(e.g. SEPT7P1,TIGAR)
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Retrieve pValues from log rank test based on raw survival analysis data
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    dataset:
                      type: object
                      description: Dataset ID and disease name.
                      properties: 
                        dataset_ID: 
                          type: integer
                          description: Internal database ID of the cancer type/dataset.
                        disease_name: 
                          type: string
                          description: Name of the cancer type.
                    gene:
                      type: object
                      description: Information about the gene.
                      properties:
                        ensg_number:
                          type: string
                          description: Ensg number of the gene.
                        gene_symbol:
                          type: string
                          description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                      required:
                      - ensg_number
                      - gene_symbol
                    pValue:
                      type: number
                      description: pValue of log rank test
  /alternativeSplicing/getTranscriptEvents:
    get:
      operationId: alternativeSplicing.get_transcript_events
      tags:
      - AlternativeSplicing
      summary: Retrieve all included events of one transcript of interest.
      description: Retrieve all included events of one transcript of interest.
      parameters:
      - name: enst_number
        in: query
        description: A comma-separated list of enst numbers of the transcript(s) of interest (e.g. ENST00000387314, ENST00000389680)
        required: true
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: Get all included events of a transcript of interest.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    alternative_splicing_event_transcripts_ID:
                      type: integer
                      description: Internal database ID of the alternative splicing event transcripts of the transcript of interest.
                    transcript:
                      type: object
                      description: Information about the transcript.
                      properties:
                        enst_number:
                          type: string
                          description: Enst number of the transcript.
                    event_name:
                      type: string
                      description: Name of the event.
                    event_type:
                      type: string
                      description: Name(s) of the event type(s)
  /alternativeSplicing/getEventPositions:
    get:
      operationId: alternativeSplicing.get_event_positions
      tags:
      - AlternativeSplicing
      summary: Retrieve the exon position(s) associated with a transcript of a specific event type.
      description: Retrieve the exon position(s) associated with a transcript of a specific event type.
      parameters:
      - name: enst_number
        in: query
        description: A enst number of a transcript of interest (e.g. ENST00000387314)
        required: true
        schema:
          type: string
      - name: event_type
        in: query
        description: Name of the event type of interest (e.g. RI, SE, MX, A5, A3, AF, AL)
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Function returns all exon position(s) for a transcript of interest and a specific event type.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    start_pos:
                      type: integer
                      description: start position of the exon
                    end_pos:
                      type: integer
                      description: end position of the exon
  /alternativeSplicing/getExonsForPosition:
    get:
      operationId: alternativeSplicing.get_exons_for_position
      tags:
      - AlternativeSplicing
      summary: Function returns exon ensembl ID(s) of an exon position of interest.
      description: Function returns exon ensembl ID(s) of an exon position of interest.
      parameters:
      - name: start_pos
        in: query
        description: start position(s) of exon(s) of interest.
        required: true
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: end_pos
        in: query
        description: end position(s) of exon(s) of interest.
        required: true
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      responses:
        "200":
          description: get exon ID(s) of exon position(s) of interest.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    transcript_element_positions:
                      type: object
                      description: Information about the exon.
                      properties:
                        start_pos:
                          type: integer
                          description: start position of the exon.
                        end_pos:
                          type: integer
                          description: end position of the exon.
                    type: 
                      type: string
                      description: Type of the exon.
                    ense_number:
                      type: string
                      description: Ense number of the exon.
  /alternativeSplicing/getPsiValues:
    get:
      operationId: alternativeSplicing.get_psi_values
      tags:
      - AlternativeSplicing
      summary: Retrieve all psi values for a specific event type of a transcript of interest.
      description: Retrieve all psi values for a specific event type of a transcript of interest.
      parameters:
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the specific cancer type/dataset. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: data_origin
        in: query
        description: Origin of the data (e.g. TCGA, GTEx, ...)
        required: false
        schema:
          type: string
      - name: transcript_ID
        in: query
        description: Internal database ID of the transcript of interest.
        required: false
        schema:
          type: integer
      - name: enst_number
        in: query
        description: A enst number of a transcript of interest (e.g. ENST00000387314)
        required: false
        schema:
          type: string
      - name: psivec_ID
        in: query
        description: Internal database ID of the psi values of the transcript of interest.
        required: false
        schema:
          type: integer
      - name: alternative_splicing_event_transcripts_ID
        in: query
        description: Internal database ID of the alternative splicing event transcripts of the transcript of interest.
        required: false
        schema:
          type: integer
      - name: sample_ID
        in: query
        description: A sample ID to filter for
        schema:
          type: string
        required: false
      - name: limit
        in: query
        description: Limit the number of results. Defaults to 100.
        schema:
          type: integer
        required: false
      - $ref: '#/components/parameters/VersionParam'
      responses:
        "200":
          description: Retrieve all psi values for a specific event type of a transcript of interest.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    psivec_ID:
                      type: integer
                      description: Internal database ID of the psi values of the transcript of interest.
                    alternative_splicing_event_transcripts:
                      type: object
                      description: Information about the alternative splicing event transcripts.
                      properties:
                        alternative_splicing_event_transcripts_ID:
                          type: integer
                          description: Internal database ID of the alternative splicing event transcripts of the transcript of interest.
                        transcript:
                          type: object
                          description: Information about the transcript.
                          properties:
                            enst_number:
                              type: string
                              description: Enst number of the transcript.
                        event_name:
                          type: string
                          description: Name of the event.
                        event_type:
                          type: string
                          description: Name(s) of the event type(s)
                    sample_ID:
                      type: string
                      description: ID of the specific sample.
                    psi_value:
                      type: number
                      description: Psi value of the specific event type.
  /spongEffects/getSpongEffectsRuns:
    get:
      operationId: spongEffects.get_spongeffects_runs
      tags: 
      - spongEffects
      summary: Get all available spongEffects runs and their dataset informations
      description: Get all available spongEffects runs and their dataset informations
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the disease (e.g. Pan-cancer, Testicular germ cell tumor, ...)
        required: false
        schema:
          type: string
      - name: include_empty_spongeffects
        in: query
        description: If true (1), datasets with no spongEffects runs are included.
        required: false
        schema:
          type: boolean
      responses:
        "200":
          description: Get all available spongEffects runs and their dataset informations
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    spongeEffects_run_ID: 
                      type: integer
                      description: Internal database ID for different SpongEffect runs. 
                    m_scor_threshold:
                      type: number
                      description: Threshold for the m-score.
                    p_adjust_threshold:
                      type: number
                      description: Threshold for the p-adjusted value.
                    modules_cutoff:
                      type: number
                      description: Threshold for the modules.
                    bin_size:
                      type: number
                      description: Threshold for the bin size.
                    min_size:
                      type: number
                      description: Threshold for the min size.
                    max_size:
                      type: number
                      description: Threshold for the max size.
                    min_expr:
                      type: number
                      description: Threshold for the min expression.
                    method:
                      type: string
                      description: Method used for the spongEffects run.
                    cv_folds: 
                      type: integer
                      description: Number of cross-validation folds.
                    level: 
                      type: string
                      description: Level used for the spongEffects run.
                    sponge_run_ID:
                      type: integer
                      description: Internal database ID for different runs with SPONGE with the same expression values for one cancer_type/dataset.
                    variance_cutoff:
                      type: number
                      description: Only consider genes and miRNA with variance > var.threshold. If this parameter is NULL no variance filtering is performed (Parameter name in SPONGE method is var.threshold)
                    f_test:
                      type: boolean
                      description: If true (1), an F-test is performed on each model parameter to assess its importance for the model based on the RSS of the full model vs the RSS of the nested model without the miRNA in question.
                    f_test_p_adj_threshold:
                      type: number
                      description: If F.test is TRUE (1), threshold to use for miRNAs to be included.
                    coefficient_threshold:
                      type: number
                      description: Threshold to cross for a regression coefficient to be called significant. Depends on the parameter coefficient.direction.
                    coefficient_direction: 
                      type: string
                      description: If "<", coefficient has to be lower than coefficient.threshold, if ">", coefficient has to be larger than threshold. If NULL, the absolute value of the coefficient has to be larger than the threshold.
                    min_corr:
                      type: number
                      description: Consider only gene pairs with a minimum correlation specified here.
                    number_of_datasets:
                      type: integer
                      description: The number of mscor coefficients to be sampled from each covariance matrix.
                    number_of_samples:
                      type: integer
                      description: The number of samples available in the expression data.
                    ks:
                      type: string
                      description: A sequence of gene-gene correlation values for which null models are computed.
                    m_max:
                      type: integer
                      description: null models are build for each elt in ks for 1 to m_max miRNAs
                    log_level:
                      type: string
                      description: The log level, can be one of "info", "debug", "error".
                    sponge_db_version: 
                      type: string
                      description: Version of the SPONGE database.
                    dataset_ID:
                      type: integer
                      description: Internal database ID of the cancer type/dataset.
                    disease_name:
                      type: string
                      description: Name of the cancer type/dataset.
                    data_origin:
                      type: string
                      description: Cancer type/dataset origin (database name, etc.).
                    disease_type:
                      type: string
                      description: Type of the disease (e.g. cancer).
                    download_url:
                      type: string
                      description: URL to cancer type/dataset annotation.
                    disease_subtype:
                      type: string
                      description: Subtype of specific cancer type (NA if no relevant subtype is present)
  /spongEffects/getRunPerformance:
    get:
      operationId: spongEffects.get_run_performance
      tags:
      - spongEffects
      summary: Get best spongEffects run performance for selected disease
      description: Get best spongEffects run performance for selected disease
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the disease (e.g. Pan-cancer, Testicular germ cell tumor, ...)
        required: false
        schema:
          type: string
      - name: level
        in: query
        description: Element level to use, either gene or transcript
        required: true
        schema:
          type: string
      - name: modules_cutoff
        in: query
        description: Threshold for the modules.
        required: false
        schema:
          type: number
      - name: p_adj_threshold
        in: query
        description: Threshold for the p-adjusted value.
        required: false
        schema:
          type: number
      - name: m_scor_threshold
        in: query
        description: Threshold for the m-score.
        required: false
        schema:
          type: number
      responses:
        "200":
          description: Successfully queried spongEffects model performance for given disease
          content:
            application/json:
              schema:
                type: object
                properties:
                  model_type:
                    type: string
                    description: spongEffects model type (Random, Central genes, or modules)
                  split_type:
                    type: string
                    description: Test or train data split
                  accuracy:
                    type: number
                    description: Overall model accuracy
                  kappa:
                    type: number
                    description: Kappa value
                  accuracy_lower:
                    type: number
                    description: Lower bound of overall accuracy
                  accuracy_upper:
                    type: number
                    description: Upper bound of overall accuracy
                  accuracy_null:
                    type: number
                    description: Null value of overall accuracy
                  accuracy_p_value:
                    type: number
                    description: P-value of overall accuracy
                  mcnemar_p_value:
                    type: number
                    description: P-value for Mcnemar value
  /spongEffects/getRunClassPerformance:
    get:
      operationId: spongEffects.get_run_class_performance
      tags:
      - spongEffects
      summary: Get detailed spongEffects model performance information for given disease type
      description: Get detailed spongEffects model performance information for given disease type
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the disease (e.g. Pan-cancer, Testicular germ cell tumor, ...)
        required: false
        schema:
          type: string
      - name: level
        in: query
        description: Level to use, i.e. gene or transcript
        required: true
        schema:
          type: string
      - name: modules_cutoff
        in: query
        description: Threshold for the modules.
        required: false
        schema:
          type: number
      - name: p_adj_threshold
        in: query
        description: Threshold for the p-adjusted value.
        required: false
        schema:
          type: number
      - name: m_scor_threshold
        in: query
        description: Threshold for the m-score.
        required: false
        schema:
          type: number
      responses:
        "200":
          description: Successfully extracted spongEffects model performances for given disease and level
          content:
            application/json:
              schema:
                type: object
                properties:
                  prediction_class:
                    type: string
                    description: Predictive class of spongEffects model
                  sensitivity:
                    type: number
                    description: Model sensitivity
                  specificity:
                    type: number
                    description: Model specificity
                  pos_pred_value:
                    type: number
                    description: Model pos_pred_value
                  neg_pred_value:
                    type: number
                    description: Model neg_pred_value
                  precision_value:
                    type: number
                    description: Model precision value
                  recall:
                    type: number
                    description: Model recall value
                  f1:
                    type: number
                    description: Model f1 value
                  prevalence:
                    type: number
                    description: Model prevalence
                  detection_rate:
                    type: number
                    description: Model detection rate
                  detection_prevalence:
                    type: number
                    description: Model detection prevalence
                  balanced_accuracy:
                    type: number
                    description: Model balanced accuracy
                  spongEffects_run:
                    type: object
                    description: spongEffects model details
                    properties:
                      model_type:
                        type: string
                        description: Model type, i.e. random, module, or central genes
                      split_type:
                        type: string
                        description: Specifies whether model was build with training or testing split
  /spongEffects/enrichmentScoreDistributions:
    get:
      operationId: spongEffects.get_enrichment_score_class_distributions
      tags:
      - spongEffects
      summary: Get enrichment score class distributions of a trained spongEffects model for a given disease
      description: Get enrichment score class distributions of a trained spongEffects model for a given disease
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the disease (e.g. Pan-cancer, Testicular germ cell tumor, ...)
        required: false
        schema:
          type: string
      - name: level
        in: query
        description: Level to use, i.e. gene or transcript
        required: true
        schema:
          type: string
      - name: modules_cutoff
        in: query
        description: Threshold for the modules.
        required: false
        schema:
          type: number
      - name: p_adj_threshold
        in: query
        description: Threshold for the p-adjusted value.
        required: false
        schema:
          type: number
      - name: m_scor_threshold
        in: query
        description: Threshold for the m-score.
        required: false
        schema:
          type: number
      responses:
        "200":
          description: Successfully calculated enrichment class distributions for given disease and level
          content:
            application/json:
              schema:
                type: object
                properties:
                  prediction_class:
                    type: string
                    description: One of the (sub-)types (predictive class) of the given disease
                  enrichment_score:
                    type: number
                    description: X-axis Enrichment score value
                  density:
                    type: number
                    description: Y-axis distribution values
  /spongEffects/getSpongEffectsGeneModules:
    get:
      operationId: spongEffects.get_gene_modules
      tags:
      - spongEffects
      summary: Extract module hub elements for a given disease, center gene, and level
      description: Extract module hub elements for a given disease, center gene, and level
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Disease name
        required: false
        schema:
          type: string
      - name: gene_ID
        in: query
        description: Internal database ID of the gene.
        required: false
        schema:
          type: integer
      - name: ensg_number
        in: query
        description: Ensembl gene ID
        required: false
        schema:
          type: string
      - name: gene_symbol
        in: query
        description: Gene symbol
        required: false
        schema:
          type: string
      - name: limit
        in: query
        description: Limit the number of results. 
        schema:
          type: integer
        required: false
      - name: offset
        in: query
        description: Offset for pagination.
        schema:
          type: integer
        required: false
      - name: modules_cutoff
        in: query
        description: Threshold for the modules.
        required: false
        schema:
          type: number
      - name: p_adj_threshold
        in: query
        description: Threshold for the p-adjusted value.
        required: false
        schema:
          type: number
      - name: m_scor_threshold
        in: query
        description: Threshold for the m-score.
        required: false
        schema:
          type: number
      responses:
        "200":
          description: Successfully extracted spongEffects module hubs for given disease
          content:
            application/json:
              schema:
                type: object
                properties:
                  spongEffects_gene_module_ID:
                    type: integer
                    description: Internal database ID of the spongEffects gene module.
                  gene:
                    type: object
                    description: Information about the gene.
                    properties:
                      ensg_number:
                        type: string
                        description: Ensg number of the gene.
                      gene_symbol:
                        type: string
                        description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                  mean_gini_decrease:
                    type: number
                    description: Mean decrease in Gini-index
                  mean_accuracy_decrease:
                    type: number
                    description: Mean decrease in accuracy
                  spongeEffects_run_ID:
                    type: integer
                    description: Internal database ID of the spongEffects run.
  /spongEffects/getSpongEffectsGeneModuleMembers:
    get:
      operationId: spongEffects.get_gene_module_members
      tags:
      - spongEffects
      summary: Get connected nodes of specified ceRNA hub(s)
      description: Get connected nodes of specified ceRNA hub(s)
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the disease
        required: false
        schema:
          type: string
      - name: gene_ID
        in: query
        description: Internal database ID of the gene.
        required: false
        schema:
          type: integer
      - name: ensg_number
        in: query
        description: Ensembl gene ID
        required: false
        schema:
          type: string
      - name: gene_symbol
        in: query
        description: Gene symbol
        required: false
        schema:
          type: string
      - name: limit
        in: query
        description: Limit the number of results. 
        schema:
          type: integer
        required: false
      - name: offset
        in: query
        description: Offset for pagination.
        schema:
          type: integer
        required: false
      responses:
        "200":
          description: Successfully extracted all module members of hub node(s)
          content:
            application/json:
              schema:
                type: object
                properties:
                  spongEffects_gene_module_members_ID:
                    type: integer
                    description: Internal database ID of the spongEffects gene module members.
                  spongEffects_gene_module_ID:
                    type: integer
                    description: Internal database ID of the spongEffects gene module.
                  gene: 
                    type: object
                    description: Information about the gene.
                    properties:
                      ensg_number:
                        type: string
                        description: Ensg number of the gene.
                      gene_symbol:
                        type: string
                        description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
  /spongEffects/getSpongEffectsGeneModuleScores:
    get: 
      operationId: spongEffects.get_gene_module_enrichment_score
      tags:
      - spongEffects
      summary: Get enrichment scores of all modules for a given gene.
      description: Get enrichment scores of all modules for a given module (spongEffects_gene_module_ID).
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: spongEffects_gene_module_ID
        in: query
        description: Internal database ID of the spongEffects gene module.
        required: true
        schema:
          type: array
          items:
            type: integer
      - name: cluster
        in: query
        description: Whether to cluster rows and columns 
        required: false
        schema:
          type: boolean
      - name: average
        in: query
        description: Whether to average scores across samples
        required: false
        schema:
          type: boolean
      responses:
        "200":
          description: Successfully extracted enrichment scores of all modules for a given gene
          content:
            application/json:
              schema:
                type: object
                properties:
                  spongEffects_gene_module_ID:
                    type: integer
                    description: Internal database ID of the spongEffects gene module.
                  sample_ID:
                    type: string
                    description: Sample ID of the specific sample.
                  enrichment_score:
                    type: number
                    description: Enrichment score of the specific module in the specific sample.
                  gene:
                    type: object
                    description: Information about the gene.
                    properties:
                      ensg_number:
                        type: string
                        description: Ensg number of the gene.
                      gene_symbol:
                        type: string
                        description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
  /spongEffects/getSpongEffectsTranscriptModules:
    get:
      operationId: spongEffects.get_transcript_modules
      tags:
      - spongEffects
      summary: Extract module hub elements for a given disease and level
      description: Extract module hub elements for a given disease and level
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: spongEffects_transcript_module_ID
        in: query
        description: Internal database ID of the spongEffects transcript module.
        required: false
        schema:
          type: integer
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Disease name
        required: false
        schema:
          type: string
      - name: gene_ID
        in: query
        description: Internal database ID of the gene.
        required: false
        schema:
          type: integer
      - name: ensg_number
        in: query
        description: Ensembl gene ID
        required: false
        schema:
          type: string
      - name: gene_symbol
        in: query
        description: Gene symbol
        required: false
        schema:
          type: string
      - name: transcript_ID
        in: query
        description: Internal database ID of the transcript.
        required: false
        schema:
          type: integer
      - name: enst_number
        in: query
        description: Ensembl transcript ID
        required: false
        schema:
          type: string
      - name: limit
        in: query
        description: Limit the number of results.
        schema:
          type: integer
        required: false
      - name: offset
        in: query
        description: Offset for pagination.
        schema:
          type: integer
        required: false
      - name: modules_cutoff
        in: query
        description: Threshold for the modules.
        required: false
        schema:
          type: number
      - name: p_adj_threshold
        in: query
        description: Threshold for the p-adjusted value.
        required: false
        schema:
          type: number
      - name: m_scor_threshold
        in: query
        description: Threshold for the m-score.
        required: false
        schema:
          type: number
      responses:
        "200":
          description: Successfully extracted spongEffects module hubs for given disease
          content:
            application/json:
              schema:
                type: object
                properties:
                  spongEffects_transcript_module_ID:
                    type: integer
                    description: Internal database ID of the spongEffects transcript module.
                  spongEffects_run_ID:
                    type: integer
                    description: Internal database ID of the spongEffects run.
                  transcript:
                    type: object
                    description: Information about the transcript.
                    properties:
                      enst_number:
                        type: string
                        description: Enst number of the transcript.
                      gene:
                        type: object
                        description: Information about the gene.
                        properties:
                          gene_symbol:
                            type: string
                            description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
                  mean_gini_decrease:
                    type: number
                    description: Mean decrease in Gini-index
                  mean_accuracy_decrease:
                    type: number
                    description: Mean decrease in accuracy
                  spongEffects_run?_ID:
                    type: integer
                    description: Internal database ID of the spongEffects run.
  /spongEffects/getSpongEffectsTranscriptModuleMembers:
    get:
      operationId: spongEffects.get_transcript_module_members
      tags:
      - spongEffects
      summary: Get connected nodes of specified ceRNA hub(s)
      description: Get connected nodes of specified ceRNA hub(s)
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: spongEffects_transcript_module_ID
        in: query
        description: Internal database ID of the spongEffects transcript module.
        required: false
        schema:
          type: integer
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the disease
        required: false
        schema:
          type: string
      - name: gene_ID
        in: query
        description: Internal database ID of the gene.
        required: false
        schema:
          type: integer
      - name: ensg_number
        in: query
        description: Ensembl gene ID
        required: false
        schema:
          type: string
      - name: gene_symbol
        in: query
        description: Gene symbol
        required: false
        schema:
          type: string
      - name: transcript_ID
        in: query
        description: Internal database ID of the transcript.
        required: false
        schema:
          type: integer
      - name: enst_number
        in: query
        description: Ensembl transcript ID
        required: false
        schema:
          type: string
      responses:
        "200":
          description: Successfully extracted all module members of hub node(s)
          content:
            application/json:
              schema:
                type: object
                properties:
                  spongEffects_transcript_module_members_ID:
                    type: integer
                    description: Internal database ID of the spongEffects transcript module members.
                  spongEffects_transcript_module_ID:
                    type: integer
                    description: Internal database ID of the spongEffects transcript module.
                  transcript:
                    type: object
                    description: Information about the transcript.
                    properties:
                      enst_number:
                        type: string
                        description: Enst number of the transcript.
                      gene:
                        type: object
                        description: Information about the gene.
                        properties:
                          gene_symbol:
                            type: string
                            description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
  /spongEffects/getSpongEffectsTranscriptModuleScores:
    get: 
      operationId: spongEffects.get_transcript_module_enrichment_score
      tags:
      - spongEffects
      summary: Get enrichment scores of all modules for a given transcript.
      description: Get enrichment scores of all modules for a given module (spongEffects_transcript_module_ID).
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: spongEffects_transcript_module_ID
        in: query
        description: Internal database ID of the spongEffects transcript module.
        required: true
        schema:
          type: array
          items:
            type: integer
      - name: cluster
        in: query
        description: Whether to cluster rows and columns 
        required: false
        schema:
          type: boolean
      - name: average
        in: query
        description: Whether to average scores across samples
        required: false
        schema:
          type: boolean
      responses:
        "200":
          description: Successfully extracted enrichment scores of all modules for a given transcript
          content:
            application/json:
              schema:
                type: object
                properties:
                  spongEffects_transcript_module_ID:
                    type: integer
                    description: Internal database ID of the spongEffects transcript module.
                  sample_ID:
                    type: string
                    description: Sample ID of the specific sample.
                  enrichment_score:
                    type: number
                  transcript:
                    type: string
                    description: Ensembl transcript ID
                  gene:
                    type: object
                    description: Information about the gene.
                    properties:
                      ensg_number:
                        type: string
                        description: Ensg number of the gene.
                      gene_symbol:
                        type: string
                        description: From official gene nomenclature commitees such as HGNC (for human). Source - Ensmble - only provided if available.
  /spongEffects/predictCancerType:
    post:
      operationId: spongEffects.upload_file
      summary: Upload a gene/transcript expression file to predict which cancer type it originated from.
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: The gene/transcript expression file to upload. Row names need to be ENSG or ENST IDs. Column names need to be sample IDs.
                subtypes:
                  type: boolean
                  description: Whether to prediction subtype level or only predict types
                log:
                  type: boolean
                  description: Whether to apply a logarithmic scaling to the uploaded expression
                mscor:
                  type: number
                  description: mscor threshold for filtering
                fdr:
                  type: number
                  description: maximum FDR rate for filtering
                min_size:
                  type: number
                  description: Minimum number of elements in a module
                max_size:
                  type: number
                  description: Maximum number of elements in a module
                min_expr:
                  type: number
                  description: Minimum number of elements present in module and expression
                method:
                  type: string
                  description: Method to use for spongEffects enrichment (GSVA, ssGSEA, or OE)
                model:
                  type: string
                  description: Use the model or a specific cancer type, e.g. 'breast_invasive_carcinoma' instead of first predicting types with the pancancer model.
      responses:
        "200":
          description: "File uploaded and processed successfully"
          content:
            application/json:
              schema:
                type: object
                properties:
                  meta:
                    type: object
                    properties:
                      runtime:
                        type: number
                        description: Total prediction runtime in seconds
                      level:
                        type: string
                        description: Element level used, i.e. either gene or transcript
                      n_samples:
                        type: number
                        description: Number of samples that were classified
                      type_predict:
                        type: string
                        description: Dominant type prediction
                      subtype_predict:
                        type: string
                        description: Dominant subtype prediction
                  data:
                    type: object
                    properties:
                      sampleID:
                        type: string
                        description: Sample ID of uploaded file
                      typePrediction:
                        type: string
                        description: Predicted TCGA cancer (sub-)type
                      subtypePrediction:
                        type: string
                        description: Predicted subtype
        "400":
          description: "File upload or processing failed"
  /comparison:
    get:
      operationId: comparison.get_comparison
      tags:
      - Comparison
      summary: Get disease comparisons
      description: Get all comparisons involving the specified disease name and the subtype.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID
        in: query
        description: Internal database ID of the cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name
        in: query
        description: Name of the disease type. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        schema:
          type: string
      - name: disease_subtype
        in: query
        description: Name of the specific subtype. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      responses:
        "200":
          description: Successfully retrieved all comparisons involving the condition with the given type name and subtype.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    comparison_ID:
                      type: integer
                      description: Id of the selected comparison.
                    condition_1:
                      type: string
                      description: Condition of the first type/dataset of the comparison (e.g. "disease" or "normal")
                    condition_2:
                      type: string
                      description: Condition of the second type/dataset of the comparison (e.g. "disease" or "normal")
                    gene_transcript:
                      type: string
                      description: Either "gene" or "transcript", depending on if the comparison was performed on gene level or transcript level data.
                    dataset_1:
                      type: object
                      description: Information about the first dataset of the comparison.
                      properties:
                        dataset_ID:
                          type: integer
                          description: Internal database ID of the type/dataset.
                        disease_name:
                          type: string
                          description: Name of the type/dataset.
                        data_origin:
                          type: string
                          description: Type/dataset origin (database name, etc.).
                        disease_type:
                          type: string
                          description: Type of the disease (e.g. cancer).
                        download_url:
                          type: string
                          description: URL to type/dataset annotation.
                        study_abbreviation:
                          type: string
                          description: Abbreviation of the disease.
                        total_number_samples:
                          type: integer
                          description: Number of samples in the dataset.
                        version:
                          type: integer
                          description: SPONGEdb version that the dataset belongs to.
                    dataset_2:
                      type: object
                      description: Information about the first dataset of the comparison.
                      properties:
                        dataset_ID:
                          type: integer
                          description: Internal databse ID of the type/dataset.
                        disease_name:
                          type: string
                          description: Name of the type/dataset.
                        data_origin:
                          type: string
                          description: Type/dataset origin (database name, etc.).
                        disease_type:
                          type: string
                          description: Type of the disease (e.g. cancer).
                        download_url:
                          type: string
                          description: URL to  type/dataset annotation.
                        study_abbreviation:
                          type: string
                          description: Abbreviation of the disease.
                        total_number_samples:
                          type: integer
                          description: Number of samples in the dataset.
                        version:
                          type: integer
                          description: SPONGEdb version that the dataset belongs to.
  /gseaSets:
    get:
      operationId: gsea.gsea_sets
      tags:
      - GSEA
      summary: Get gene sets.
      description: Get all gene sets for which GSEA results are available for the given type, subtype and condition combination.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID_1
        in: query
        description: Internal database ID of the first cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: dataset_ID_2
        in: query
        description: Internal database ID of the second cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name_1
        in: query
        description: Name of the disease type for the first part of the GSEA analysis. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_name_2
        in: query
        description: Name of the disease type for the second part of the GSEA analysis. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_subtype_1
        in: query
        description: Name of the specific subtype for the first part of the GSEA analysis. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      - name: disease_subtype_2
        in: query
        description: Name of the specific subtype for the second part of the GSEA analysis. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      - name: condition_1
        in: query
        description: Condition of the first part of the GSEA analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: condition_2
        in: query
        description: Condition of the second dataset of the GSEA analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string

      responses:
        "200":
          description: Successfully retrieved all gene sets for which a GSEA result exists for the given comparison.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    gene_set:
                      type: string
                      description: Name of the gene set.
  /gseaTerms:
    get:
      operationId: gsea.gsea_terms
      tags:
      - GSEA
      summary: Get GSEA terms.
      description: Get all terms for which GSEA results are available for the given gene set, type/subtype, and condition combination.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID_1
        in: query
        description: Internal database ID of the first cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: dataset_ID_2
        in: query
        description: Internal database ID of the second cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name_1
        in: query
        description: Name of the disease type for the first part of the GSEA analysis. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_name_2
        in: query
        description: Name of the disease type for the second part of the GSEA analysis. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_subtype_1
        in: query
        description: Name of the specific subtype for the first part of the GSEA analysis. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      - name: disease_subtype_2
        in: query
        description: Name of the specific subtype for the second part of the GSEA analysis. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      - name: condition_1
        in: query
        description: Condition of the first part of the GSEA analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: condition_2
        in: query
        description: Condition of the second dataset of the GSEA analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: gene_set
        in: query
        description: Gene set to which the term belongs.
        required: true
        schema:
          type: string

      responses:
        "200":
          description: Successfully retrieved all terms for the given comparison and gene set.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    gene_set:
                      type: string
                      description: Name of the term.
  /gseaResults:
    get:
      operationId: gsea.gsea_results
      tags:
      - GSEA
      summary: Get GSEA results.
      description: Get GSEA results for the given gene set, type, subtype, and condition combination.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID_1
        in: query
        description: Internal database ID of the first cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: dataset_ID_2
        in: query
        description: Internal database ID of the second cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name_1
        in: query
        description: Name of the disease type for the first part of the GSEA analysis. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_name_2
        in: query
        description: Name of the disease type for the second part of the GSEA analysis. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_subtype_1
        in: query
        description: Name of the specific subtype for the first part of the GSEA analysis. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      - name: disease_subtype_2
        in: query
        description: Name of the specific subtype for the second part of the GSEA analysis. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      - name: condition_1
        in: query
        description: Condition of the first part of the GSEA analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: condition_2
        in: query
        description: Condition of the second dataset of the GSEA analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: gene_set
        in: query
        description: Gene set that the result belongs to.
        required: true
        schema:
          type: string
      - name: term
        in: query
        description: Term that the result belongs to. Fuzzy search is available (e.g. "GO:1902533").
        required: false
        schema:
          type: string
      responses:
        "200":
          description: Successfully retrieved GSEA results for the given comparison, gene set and term.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    es:
                      type: number
                      description: Enrichment Score.
                    nes:
                      type: number
                      description: Normalised Enrichment Score.
                    pvalue:
                      type: number
                      description: P-Value of GSEA result.
                    fdr:
                      type: number
                      description: P-Value after false discovery rate control
                    fwerp:
                      type: number
                      description: P-Value after family wise error rate control
                    gene_percent:
                      type: number
                      description: Gene Percent.
                    tag_percent:
                      type: string
                      description: (Genes in leading edge)/(Total matched genes).
                    term:
                      type: string
                      description: Name of the term.
                    lead_genes:
                      type: object
                      properties:
                        gsea_lead_genes_ID:
                          type: integer
                          description: Internal database ID of the leading edge gene.
                        gene:
                          type: object
                          properties:
                            ensg_number:
                              type: string
                              description: Ensembl gene ID of the leading edge gene.
                            gene_symbol:
                              type: string
                              description: Gene symbol of the leading edge gene.
                    matched_genes:
                      type: object
                      properties:
                        gsea_matched_genes_ID:
                          type: integer
                          description: Internal database ID of the matched gene.
                        gene:
                          type: object
                          properties:
                            ensg_number:
                              type: string
                              description: Ensembl gene ID of the matched gene.
                            gene_symbol:
                              type: string
                              description: Gene symbol of the matched gene.
                    res:
                      type: object
                      properties:
                        res_ID:
                          type: integer
                          description: Id of the score within the gsea result, creates an order for the scores.
                        score: 
                          type: number
                          description: Value of the score.               
  /gseaPlot:
    get:
      operationId: gsea.gsea_plot
      tags:
      - GSEA
      summary: Get GSEA plot.
      description: Get GSEA plot for the given gene set, type/subtype, and condition combination.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID_1
        in: query
        description: Internal database ID of the first cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: dataset_ID_2
        in: query
        description: Internal database ID of the second cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name_1
        in: query
        description: Name of the disease type for the first part of the GSEA analysis. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_name_2
        in: query
        description: Name of the disease type for the second part of the GSEA analysis. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_subtype_1
        in: query
        description: Name of the specific subtype for the first part of the GSEA analysis. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      - name: disease_subtype_2
        in: query
        description: Name of the specific subtype for the second part of the GSEA analysis. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      - name: condition_1
        in: query
        description: Condition of the first part of the GSEA analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: condition_2
        in: query
        description: Condition of the second dataset of the GSEA analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: gene_set
        in: query
        description: Gene set that the plot belongs to.
        required: true
        schema:
          type: string
      - name: term
        in: query
        description: Term that the plot belongs to. Fuzzy search is available (e.g. "GO:1902533").
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Successfully retrieved GSEA results for the given comparison, term, and gene set.
          content:
            application/json:
              schema:
                type: string
                description: Base64 encoded string containing GSEA plot
  /differentialExpression:
    get:
      operationId: diffExpression.get_diff_expr
      tags:
      - Differential Expression
      summary: Get differential expression results between genes.
      description: Get differential expression results for the given gene, type, subtype, and condition combination.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID_1
        in: query
        description: Internal database ID of the first cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: dataset_ID_2
        in: query
        description: Internal database ID of the second cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: condition_1
        in: query
        description: Condition of the first part of the differential expression analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: condition_2
        in: query
        description: Condition of the second dataset of the differential expression analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: ensg_number
        in: query
        description: Comma seperated ENSG numbers of the genes.
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: gene_symbol
        in: query
        description: Comma seperated gene symbols.
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form
      - name: limit
        in: query
        description: Limit the number of results. 
        schema:
          type: integer
        required: false
      - name: offset
        in: query
        description: Offset for pagination.
        schema:
          type: integer
        required: false
      responses:
        "200":
          description: Successfully retrieved differential expression results for the given gene, type, subtype, and condition combination.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    differential_expression_gene_ID: 
                      type: integer
                      description: Id of differential expression comparison in the database.
                    gene:
                      type: string
                      description: Gene Symbol.
                    baseMean:
                      type: number
                      description: Differential Expression Base Mean.
                    log2FoldChange:
                      type: number
                      description: Log2 Fold Change between gene expressions.
                    pvalue:
                      type: number
                      description: P-Value for change between gene expressions.
                    padj:
                      type: number
                      description: P-Value adjusted for multiple testing problem.
                    stat:
                      type: number
                      description: Differential Expression Statistic.
                    lfcSE:
                      type: number
                      description: Standard Error of Log Fold Change.
  /differentialExpressionTranscript:
    get:
      operationId: diffExpression.get_diff_expr_transcript
      tags:
      - Differential Expression
      summary: Get differential expression results between transcripts.
      description: Get differential expression results for the given transcript, type, subtype, and condition combination.
      parameters:
      - $ref: '#/components/parameters/VersionParam'
      - name: dataset_ID_1
        in: query
        description: Internal database ID of the first cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: dataset_ID_2
        in: query
        description: Internal database ID of the second cancer type/dataset.
        required: false
        schema:
          type: integer
      - name: disease_name_1
        in: query
        description: Name of the disease type for the first part of the differential expression analysis. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_name_2
        in: query
        description: Name of the disease type for the second part of the differential expression analysis. Fuzzy search is available (e.g. "kidney clear cell carcinoma" or just "kidney").
        required: false
        schema:
          type: string
      - name: disease_subtype_1
        in: query
        description: Name of the specific subtype for the first part of the differential expression analysis. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      - name: disease_subtype_2
        in: query
        description: Name of the specific subtype for the second part of the differential expression analysis. Fuzzy search is available (e.g. "LMS").
        schema:
          type: string
      - name: condition_1
        in: query
        description: Condition of the first part of the differential expression analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: condition_2
        in: query
        description: Condition of the second dataset of the differential expression analysis (e.g. "disease" or "normal").
        required: true
        schema:
          type: string
      - name: ensg_number
        in: query
        description: Comma seperated ENST numbers of the transcripts.
        required: false
        schema:
          type: array
          items:
            type: string
          minItems: 0
        explode: false
        style: form

      responses:
        "200":
          description: Successfully retrieved differential expression results for the given transcript, type, subtype, and condition combination.
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    differential_expression_gene_ID: 
                      type: integer
                      description: Id of differential expression comparison in the database.
                    transcript:
                      type: string
                      description: ENST number of transcript.
                    baseMean:
                      type: number
                      description: Differential Expression Base Mean.
                    log2FoldChange:
                      type: number
                      description: Log2 Fold Change between gene expressions.
                    pvalue:
                      type: number
                      description: P-Value for change between gene expressions.
                    padj:
                      type: number
                      description: P-Value adjusted for multiple testing problem.
                    stat:
                      type: number
                      description: Differential Expression Statistic.
                    lfcSE:
                      type: number
                      description: Standard Error of Log Fold Change.
